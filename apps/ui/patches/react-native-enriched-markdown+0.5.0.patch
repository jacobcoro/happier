diff --git a/node_modules/react-native-enriched-markdown/android/generated/jni/react/renderer/components/EnrichedMarkdownTextSpec/Props.h b/node_modules/react-native-enriched-markdown/android/generated/jni/react/renderer/components/EnrichedMarkdownTextSpec/Props.h
index c9e1984..4f75d3a 100644
--- a/node_modules/react-native-enriched-markdown/android/generated/jni/react/renderer/components/EnrichedMarkdownTextSpec/Props.h
+++ b/node_modules/react-native-enriched-markdown/android/generated/jni/react/renderer/components/EnrichedMarkdownTextSpec/Props.h
@@ -1857,6 +1857,7 @@ static inline folly::dynamic toDynamic(const EnrichedMarkdownMarkdownStyleStruct
 struct EnrichedMarkdownMd4cFlagsStruct {
   bool underline{false};
   bool latexMath{false};
+  bool texMathBackslashDelimiters{false};
 
 
 #ifdef RN_SERIALIZABLE_STATE
@@ -1866,6 +1867,7 @@ struct EnrichedMarkdownMd4cFlagsStruct {
     folly::dynamic result = folly::dynamic::object();
     result["underline"] = underline;
     result["latexMath"] = latexMath;
+    result["texMathBackslashDelimiters"] = texMathBackslashDelimiters;
     return result;
   }
 #endif
@@ -1882,6 +1884,10 @@ static inline void fromRawValue(const PropsParserContext& context, const RawValu
   if (tmp_latexMath != map.end()) {
     fromRawValue(context, tmp_latexMath->second, result.latexMath);
   }
+  auto tmp_texMathBackslashDelimiters = map.find("texMathBackslashDelimiters");
+  if (tmp_texMathBackslashDelimiters != map.end()) {
+    fromRawValue(context, tmp_texMathBackslashDelimiters->second, result.texMathBackslashDelimiters);
+  }
 }
 
 static inline std::string toString(const EnrichedMarkdownMd4cFlagsStruct &value) {
@@ -4153,6 +4159,7 @@ static inline folly::dynamic toDynamic(const EnrichedMarkdownTextMarkdownStyleSt
 struct EnrichedMarkdownTextMd4cFlagsStruct {
   bool underline{false};
   bool latexMath{false};
+  bool texMathBackslashDelimiters{false};
 
 
 #ifdef RN_SERIALIZABLE_STATE
@@ -4162,6 +4169,7 @@ struct EnrichedMarkdownTextMd4cFlagsStruct {
     folly::dynamic result = folly::dynamic::object();
     result["underline"] = underline;
     result["latexMath"] = latexMath;
+    result["texMathBackslashDelimiters"] = texMathBackslashDelimiters;
     return result;
   }
 #endif
@@ -4178,6 +4186,10 @@ static inline void fromRawValue(const PropsParserContext& context, const RawValu
   if (tmp_latexMath != map.end()) {
     fromRawValue(context, tmp_latexMath->second, result.latexMath);
   }
+  auto tmp_texMathBackslashDelimiters = map.find("texMathBackslashDelimiters");
+  if (tmp_texMathBackslashDelimiters != map.end()) {
+    fromRawValue(context, tmp_texMathBackslashDelimiters->second, result.texMathBackslashDelimiters);
+  }
 }
 
 static inline std::string toString(const EnrichedMarkdownTextMd4cFlagsStruct &value) {
diff --git a/node_modules/react-native-enriched-markdown/android/src/main/cpp/jni-adapter.cpp b/node_modules/react-native-enriched-markdown/android/src/main/cpp/jni-adapter.cpp
index 16137fa..0eb4004 100644
--- a/node_modules/react-native-enriched-markdown/android/src/main/cpp/jni-adapter.cpp
+++ b/node_modules/react-native-enriched-markdown/android/src/main/cpp/jni-adapter.cpp
@@ -206,6 +206,10 @@ JNIEXPORT jobject JNICALL Java_com_swmansion_enriched_markdown_parser_Parser_nat
         if (latexMathField) {
           md4cFlags.latexMath = env->GetBooleanField(flags, latexMathField) == JNI_TRUE;
         }
+        jfieldID texMathBackslashDelimitersField = env->GetFieldID(flagsClass, "texMathBackslashDelimiters", "Z");
+        if (texMathBackslashDelimitersField) {
+          md4cFlags.texMathBackslashDelimiters = env->GetBooleanField(flags, texMathBackslashDelimitersField) == JNI_TRUE;
+        }
         jfieldID permissiveAutolinksField = env->GetFieldID(flagsClass, "permissiveAutolinks", "Z");
         if (permissiveAutolinksField) {
           md4cFlags.permissiveAutolinks = env->GetBooleanField(flags, permissiveAutolinksField) == JNI_TRUE;
diff --git a/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/MeasurementStore.kt b/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/MeasurementStore.kt
index 0abbba9..e101f37 100644
--- a/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/MeasurementStore.kt
+++ b/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/MeasurementStore.kt
@@ -245,6 +245,7 @@ object MeasurementStore {
       Md4cFlags(
         underline = props.getMapOrNull("md4cFlags").getBooleanOrDefault("underline", false),
         latexMath = FeatureFlags.IS_MATH_ENABLED && props.getMapOrNull("md4cFlags").getBooleanOrDefault("latexMath", true),
+        texMathBackslashDelimiters = FeatureFlags.IS_MATH_ENABLED && props.getMapOrNull("md4cFlags").getBooleanOrDefault("texMathBackslashDelimiters", false),
       )
 
     val fontSize = getInitialFontSize(styleMap, context, allowFontScaling, fontScale, maxFontSizeMultiplier)
@@ -295,6 +296,7 @@ object MeasurementStore {
       Md4cFlags(
         underline = props.getMapOrNull("md4cFlags").getBooleanOrDefault("underline", false),
         latexMath = FeatureFlags.IS_MATH_ENABLED && props.getMapOrNull("md4cFlags").getBooleanOrDefault("latexMath", true),
+        texMathBackslashDelimiters = FeatureFlags.IS_MATH_ENABLED && props.getMapOrNull("md4cFlags").getBooleanOrDefault("texMathBackslashDelimiters", false),
       )
     val allowTrailingMargin = props.getBooleanOrDefault("allowTrailingMargin", false)
     val propsHash = computePropsHash(props, allowFontScaling, fontScale, maxFontSizeMultiplier)
diff --git a/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/parser/Parser.kt b/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/parser/Parser.kt
index 08b031d..d817f1b 100644
--- a/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/parser/Parser.kt
+++ b/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/parser/Parser.kt
@@ -6,6 +6,7 @@ import com.swmansion.enriched.markdown.utils.common.FeatureFlags
 data class Md4cFlags(
   val underline: Boolean = false,
   val latexMath: Boolean = FeatureFlags.IS_MATH_ENABLED,
+  val texMathBackslashDelimiters: Boolean = false,
   val permissiveAutolinks: Boolean = true,
 ) {
   companion object {
diff --git a/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/utils/common/MarkdownViewManagerUtils.kt b/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/utils/common/MarkdownViewManagerUtils.kt
index 9f3152d..38fd365 100644
--- a/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/utils/common/MarkdownViewManagerUtils.kt
+++ b/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/utils/common/MarkdownViewManagerUtils.kt
@@ -82,6 +82,7 @@ fun parseMd4cFlags(flags: ReadableMap?): Md4cFlags =
   Md4cFlags(
     underline = flags?.getBoolean("underline") ?: false,
     latexMath = FeatureFlags.IS_MATH_ENABLED && (flags?.getBoolean("latexMath") ?: true),
+    texMathBackslashDelimiters = FeatureFlags.IS_MATH_ENABLED && (flags?.getBoolean("texMathBackslashDelimiters") ?: false),
   )
 
 fun parseContextMenuItems(value: ReadableArray?): List<String> =
diff --git a/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/utils/text/TailFadeInAnimator.kt b/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/utils/text/TailFadeInAnimator.kt
index 9feea24..7c5d540 100644
--- a/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/utils/text/TailFadeInAnimator.kt
+++ b/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/utils/text/TailFadeInAnimator.kt
@@ -4,6 +4,7 @@ import android.animation.Animator
 import android.animation.AnimatorListenerAdapter
 import android.animation.ValueAnimator
 import android.text.Spannable
+import android.view.animation.AnimationUtils
 import android.view.animation.LinearInterpolator
 import android.widget.TextView
 import com.swmansion.enriched.markdown.spans.FadeInSpan
@@ -14,58 +15,124 @@ class TailFadeInAnimator(
 ) {
   private val viewRef = WeakReference(textView)
 
-  private val activeAnimations = mutableMapOf<FadeInSpan, ValueAnimator>()
+  private data class ActiveFadeRange(
+    val start: Int,
+    val end: Int,
+    val startedAtMs: Long,
+  )
+
+  private val activeRanges = mutableListOf<ActiveFadeRange>()
+  private val activeSpans = mutableListOf<FadeInSpan>()
+  private var frameAnimator: ValueAnimator? = null
 
   fun animate(
     tailStart: Int,
     tailEnd: Int,
   ) {
-    if (tailEnd <= tailStart) return
+    val textView = viewRef.get() ?: return
+    val spannable = textView.text as? Spannable ?: return
+    val nowMs = AnimationUtils.currentAnimationTimeMillis()
 
+    pruneCompletedRanges(nowMs, spannable.length)
+    if (tailEnd > tailStart && tailEnd <= spannable.length) {
+      activeRanges += ActiveFadeRange(
+        start = tailStart,
+        end = tailEnd,
+        startedAtMs = nowMs,
+      )
+    }
+
+    applyActiveRanges(nowMs)
+    scheduleFrameAnimator(nowMs)
+  }
+
+  private fun applyActiveRanges(nowMs: Long) {
     val textView = viewRef.get() ?: return
     val spannable = textView.text as? Spannable ?: return
 
-    val fadeSpan = FadeInSpan().apply { alpha = 0f }
-    spannable.setSpan(fadeSpan, tailStart, tailEnd, Spannable.SPAN_EXCLUSIVE_EXCLUSIVE)
+    activeSpans.forEach { spannable.removeSpan(it) }
+    activeSpans.clear()
+
+    activeRanges.forEach { range ->
+      if (range.end > spannable.length) return@forEach
+      val elapsedMs = nowMs - range.startedAtMs
+      val progress = (elapsedMs.toFloat() / FADE_DURATION_MS.toFloat()).coerceIn(0f, 1f)
+      val fadeSpan = FadeInSpan().apply { alpha = progress }
+      activeSpans += fadeSpan
+      spannable.setSpan(fadeSpan, range.start, range.end, Spannable.SPAN_EXCLUSIVE_EXCLUSIVE)
+    }
+
+    textView.invalidate()
+  }
 
-    val animator =
+  private fun pruneCompletedRanges(
+    nowMs: Long,
+    textLength: Int,
+  ) {
+    activeRanges.removeAll { range ->
+      range.end > textLength || nowMs - range.startedAtMs >= FADE_DURATION_MS
+    }
+  }
+
+  private fun scheduleFrameAnimator(nowMs: Long) {
+    val remainingMs =
+      activeRanges.maxOfOrNull { range ->
+        FADE_DURATION_MS - (nowMs - range.startedAtMs)
+      } ?: run {
+        cleanupSpans()
+        frameAnimator?.cancel()
+        frameAnimator = null
+        return
+      }
+
+    frameAnimator?.removeAllUpdateListeners()
+    frameAnimator?.removeAllListeners()
+    frameAnimator?.cancel()
+    frameAnimator =
       ValueAnimator.ofFloat(0f, 1f).apply {
-        duration = FADE_DURATION_MS
+        duration = remainingMs.coerceAtLeast(1L)
         interpolator = LinearInterpolator()
 
-        addUpdateListener { anim ->
-          fadeSpan.alpha = anim.animatedValue as Float
-          viewRef.get()?.invalidate()
+        addUpdateListener {
+          val frameNowMs = AnimationUtils.currentAnimationTimeMillis()
+          pruneCompletedRanges(frameNowMs, (viewRef.get()?.text as? Spannable)?.length ?: 0)
+          applyActiveRanges(frameNowMs)
         }
 
         addListener(
           object : AnimatorListenerAdapter() {
             override fun onAnimationEnd(animation: Animator) {
-              cleanup(fadeSpan)
-            }
-
-            override fun onAnimationCancel(animation: Animator) {
-              cleanup(fadeSpan)
+              val frameNowMs = AnimationUtils.currentAnimationTimeMillis()
+              pruneCompletedRanges(frameNowMs, (viewRef.get()?.text as? Spannable)?.length ?: 0)
+              applyActiveRanges(frameNowMs)
+              if (activeRanges.isEmpty()) {
+                cleanupSpans()
+                frameAnimator = null
+              } else {
+                scheduleFrameAnimator(frameNowMs)
+              }
             }
           },
         )
       }
-
-    activeAnimations[fadeSpan] = animator
-    animator.start()
+    frameAnimator?.start()
   }
 
-  private fun cleanup(span: FadeInSpan) {
-    activeAnimations.remove(span)
-    val spannable = viewRef.get()?.text as? Spannable ?: return
-    span.alpha = 1f
-    spannable.removeSpan(span)
+  private fun cleanupSpans() {
+    val spannable = viewRef.get()?.text as? Spannable
+    if (spannable != null) {
+      activeSpans.forEach { spannable.removeSpan(it) }
+    }
+    activeSpans.clear()
   }
 
   fun cancelAll() {
-    val spans = activeAnimations.keys.toList()
-    spans.forEach { activeAnimations[it]?.cancel() }
-    activeAnimations.clear()
+    frameAnimator?.removeAllUpdateListeners()
+    frameAnimator?.removeAllListeners()
+    frameAnimator?.cancel()
+    frameAnimator = null
+    activeRanges.clear()
+    cleanupSpans()
   }
 
   companion object {
diff --git a/node_modules/react-native-enriched-markdown/cpp/md4c/md4c.c b/node_modules/react-native-enriched-markdown/cpp/md4c/md4c.c
index 76a66d2..197ec38 100644
--- a/node_modules/react-native-enriched-markdown/cpp/md4c/md4c.c
+++ b/node_modules/react-native-enriched-markdown/cpp/md4c/md4c.c
@@ -201,7 +201,7 @@ struct MD_CTX_tag {
 #endif
 
     /* For resolving of inline spans. */
-    MD_MARKSTACK opener_stacks[17];
+    MD_MARKSTACK opener_stacks[19];
 #define ASTERISK_OPENERS_oo_mod3_0      (ctx->opener_stacks[0])     /* Opener-only */
 #define ASTERISK_OPENERS_oo_mod3_1      (ctx->opener_stacks[1])
 #define ASTERISK_OPENERS_oo_mod3_2      (ctx->opener_stacks[2])
@@ -219,6 +219,8 @@ struct MD_CTX_tag {
 #define BRACKET_OPENERS                 (ctx->opener_stacks[14])
 #define DOLLAR_OPENERS                  (ctx->opener_stacks[15])
 #define PIPE_OPENERS                    (ctx->opener_stacks[16])
+#define BACKSLASH_TEX_INLINE_OPENERS        (ctx->opener_stacks[17])
+#define BACKSLASH_TEX_DISPLAY_OPENERS       (ctx->opener_stacks[18])
 
     /* Stack of dummies which need to call free() for pointers stored in them.
      * These are constructed during inline parsing and freed after all the block
@@ -2553,6 +2555,7 @@ struct MD_MARK_tag {
 #define MD_MARK_AUTOLINK_MISSING_MAILTO     0x40
 #define MD_MARK_VALIDPERMISSIVEAUTOLINK     0x20  /* For permissive autolinks. */
 #define MD_MARK_HASNESTEDBRACKETS           0x20  /* For '[' to rule out invalid link labels early */
+#define MD_MARK_BACKSLASH_TEX_DISPLAY            0x20  /* For '\\' backslash-delimited math delimiters. */
 
 static MD_MARKSTACK*
 md_emph_stack(MD_CTX* ctx, MD_CHAR ch, unsigned flags)
@@ -3041,6 +3044,49 @@ md_collect_marks(MD_CTX* ctx, const MD_LINE* lines, MD_SIZE n_lines, int table_m
 
             ch = CH(off);
 
+            /* Backslash-delimited TeX math delimiters. Handle these before CommonMark
+             * backslash escapes so incomplete input can remain literal until
+             * a matching delimiter arrives. */
+            if(ch == _T('\\')  &&  (ctx->parser.flags & MD_FLAG_BACKSLASHTEXMATHSPANS)  &&  off+1 < line->end) {
+                CHAR delimiter = CH(off+1);
+
+                if(delimiter == _T('(')  ||  delimiter == _T(')')) {
+                    ADD_MARK(ch, off, off+2,
+                             delimiter == _T('(') ? MD_MARK_POTENTIAL_OPENER : MD_MARK_POTENTIAL_CLOSER);
+                    off += 2;
+                    continue;
+                }
+
+                if(delimiter == _T('[')  ||  delimiter == _T(']')) {
+                    OFF cursor;
+                    int standalone = TRUE;
+
+                    if(delimiter == _T('[')) {
+                        for(cursor = line->beg; cursor < off; cursor++) {
+                            if(!ISBLANK(cursor)) {
+                                standalone = FALSE;
+                                break;
+                            }
+                        }
+                    } else {
+                        for(cursor = off+2; cursor < line->end; cursor++) {
+                            if(!ISBLANK(cursor)) {
+                                standalone = FALSE;
+                                break;
+                            }
+                        }
+                    }
+
+                    if(standalone) {
+                        ADD_MARK(ch, off, off+2,
+                                 (delimiter == _T('[') ? MD_MARK_POTENTIAL_OPENER : MD_MARK_POTENTIAL_CLOSER) |
+                                     MD_MARK_BACKSLASH_TEX_DISPLAY);
+                        off += 2;
+                        continue;
+                    }
+                }
+            }
+
             /* A backslash escape.
              * It can go beyond line->end as it may involve escaped new
              * line to form a hard break. */
@@ -3867,6 +3913,39 @@ md_analyze_dollar(MD_CTX* ctx, int mark_index)
         md_mark_stack_push(ctx, &DOLLAR_OPENERS, mark_index);
 }
 
+static void
+md_analyze_backslash_tex_math(MD_CTX* ctx, int mark_index)
+{
+    MD_MARK* mark = &ctx->marks[mark_index];
+    int is_display = (mark->flags & MD_MARK_BACKSLASH_TEX_DISPLAY) != 0;
+    MD_MARKSTACK* stack = is_display ? &BACKSLASH_TEX_DISPLAY_OPENERS : &BACKSLASH_TEX_INLINE_OPENERS;
+
+    if((mark->flags & MD_MARK_POTENTIAL_CLOSER)  &&  stack->top >= 0) {
+        int opener_index = md_mark_stack_pop(ctx, stack);
+        MD_MARK* opener = &ctx->marks[opener_index];
+        OFF off;
+        int has_content = FALSE;
+        int crosses_line = FALSE;
+
+        for(off = opener->end; off < mark->beg; off++) {
+            if(ISNEWLINE(off))
+                crosses_line = TRUE;
+            else if(!ISUNICODEWHITESPACE(off))
+                has_content = TRUE;
+        }
+
+        if(has_content  &&  (is_display || !crosses_line)) {
+            md_rollback(ctx, opener_index, mark_index, MD_ROLLBACK_ALL);
+            md_resolve_range(ctx, opener_index, mark_index);
+            stack->top = -1;
+            return;
+        }
+    }
+
+    if(mark->flags & MD_MARK_POTENTIAL_OPENER)
+        md_mark_stack_push(ctx, stack, mark_index);
+}
+
 static void
 md_analyze_pipe(MD_CTX* ctx, int mark_index)
 {
@@ -4128,6 +4207,7 @@ md_analyze_marks(MD_CTX* ctx, const MD_LINE* lines, MD_SIZE n_lines,
             case '*':   md_analyze_emph(ctx, i); break;
             case '~':   md_analyze_tilde(ctx, i); break;
             case '$':   md_analyze_dollar(ctx, i); break;
+            case '\\':  md_analyze_backslash_tex_math(ctx, i); break;
             case '.':   /* Pass through. */
             case ':':   /* Pass through. */
             case '@':   md_analyze_permissive_autolink(ctx, i); break;
@@ -4208,6 +4288,10 @@ md_analyze_link_contents(MD_CTX* ctx, const MD_LINE* lines, MD_SIZE n_lines,
         md_analyze_marks(ctx, lines, n_lines, mark_beg, mark_end, _T("@:."), MD_ANALYZE_NOSKIP_EMPH);
     }
 
+    /* Let the existing autolink owner claim URL ranges before backslash-delimited
+     * math. NOSKIP_EMPH still permits math nested inside resolved emphasis. */
+    md_analyze_marks(ctx, lines, n_lines, mark_beg, mark_end, _T("\\"), MD_ANALYZE_NOSKIP_EMPH);
+
     for(i = 0; i < (int) SIZEOF_ARRAY(ctx->opener_stacks); i++)
         ctx->opener_stacks[i].top = -1;
 }
@@ -4297,11 +4381,24 @@ md_process_inlines(MD_CTX* ctx, const MD_LINE* lines, MD_SIZE n_lines)
         /* If reached the mark, process it and move to next one. */
         if(off >= mark->beg) {
             switch(mark->ch) {
-                case '\\':      /* Backslash escape. */
-                    if(ISNEWLINE(mark->beg+1))
+                case '\\':      /* Backslash escape or backslash-delimited TeX math. */
+                    if((mark->flags & MD_MARK_OPENER) != 0) {
+                        MD_ENTER_SPAN((mark->flags & MD_MARK_BACKSLASH_TEX_DISPLAY) != 0
+                                          ? MD_SPAN_LATEXMATH_DISPLAY
+                                          : MD_SPAN_LATEXMATH,
+                                      NULL);
+                        text_type = MD_TEXT_LATEXMATH;
+                    } else if((mark->flags & MD_MARK_CLOSER) != 0) {
+                        MD_LEAVE_SPAN((mark->flags & MD_MARK_BACKSLASH_TEX_DISPLAY) != 0
+                                          ? MD_SPAN_LATEXMATH_DISPLAY
+                                          : MD_SPAN_LATEXMATH,
+                                      NULL);
+                        text_type = MD_TEXT_NORMAL;
+                    } else if(ISNEWLINE(mark->beg+1)) {
                         enforce_hardbreak = 1;
-                    else
+                    } else {
                         MD_TEXT(text_type, STR(mark->beg+1), 1);
+                    }
                     break;
 
                 case ' ':       /* Non-trivial space. */
@@ -4511,8 +4608,8 @@ md_process_inlines(MD_CTX* ctx, const MD_LINE* lines, MD_SIZE n_lines)
 
             if(text_type == MD_TEXT_CODE || text_type == MD_TEXT_LATEXMATH) {
                 MD_ASSERT(prev_mark != NULL);
-                MD_ASSERT(ISANYOF2_(prev_mark->ch, '`', '$')  &&  (prev_mark->flags & MD_MARK_OPENER));
-                MD_ASSERT(ISANYOF2_(mark->ch, '`', '$')  &&  (mark->flags & MD_MARK_CLOSER));
+                MD_ASSERT(ISANYOF3_(prev_mark->ch, '`', '$', '\\')  &&  (prev_mark->flags & MD_MARK_OPENER));
+                MD_ASSERT(ISANYOF3_(mark->ch, '`', '$', '\\')  &&  (mark->flags & MD_MARK_CLOSER));
 
                 /* Inside a code span, trailing line whitespace has to be
                  * outputted. */
diff --git a/node_modules/react-native-enriched-markdown/cpp/md4c/md4c.h b/node_modules/react-native-enriched-markdown/cpp/md4c/md4c.h
index 00f8cc7..f949146 100644
--- a/node_modules/react-native-enriched-markdown/cpp/md4c/md4c.h
+++ b/node_modules/react-native-enriched-markdown/cpp/md4c/md4c.h
@@ -321,6 +321,7 @@ typedef struct MD_SPAN_WIKILINK {
 #define MD_FLAG_UNDERLINE 0x4000        /* Enable underline extension (and disables '_' for normal emphasis). */
 #define MD_FLAG_HARD_SOFT_BREAKS 0x8000 /* Force all soft breaks to act as hard breaks. */
 #define MD_FLAG_SPOILER 0x10000         /* Enable ||hidden text|| spoiler spans. */
+#define MD_FLAG_BACKSLASHTEXMATHSPANS 0x20000 /* Enable backslash-delimited \(...\) and standalone \[...\] equations. */
 
 #define MD_FLAG_PERMISSIVEAUTOLINKS                                                                                    \
   (MD_FLAG_PERMISSIVEEMAILAUTOLINKS | MD_FLAG_PERMISSIVEURLAUTOLINKS | MD_FLAG_PERMISSIVEWWWAUTOLINKS)
diff --git a/node_modules/react-native-enriched-markdown/cpp/parser/MD4CParser.cpp b/node_modules/react-native-enriched-markdown/cpp/parser/MD4CParser.cpp
index bbc0b0e..91f6a9c 100644
--- a/node_modules/react-native-enriched-markdown/cpp/parser/MD4CParser.cpp
+++ b/node_modules/react-native-enriched-markdown/cpp/parser/MD4CParser.cpp
@@ -474,6 +474,9 @@ std::shared_ptr<MarkdownASTNode> MD4CParser::parse(const std::string &markdown,
   if (md4cFlags.latexMath) {
     flags |= MD_FLAG_LATEXMATHSPANS;
   }
+  if (md4cFlags.texMathBackslashDelimiters) {
+    flags |= MD_FLAG_BACKSLASHTEXMATHSPANS;
+  }
   if (md4cFlags.underline) {
     flags |= MD_FLAG_UNDERLINE;
   }
diff --git a/node_modules/react-native-enriched-markdown/cpp/parser/MD4CParser.hpp b/node_modules/react-native-enriched-markdown/cpp/parser/MD4CParser.hpp
index 6d5eb2f..d3ae3c6 100644
--- a/node_modules/react-native-enriched-markdown/cpp/parser/MD4CParser.hpp
+++ b/node_modules/react-native-enriched-markdown/cpp/parser/MD4CParser.hpp
@@ -9,6 +9,7 @@ namespace Markdown {
 struct Md4cFlags {
     bool underline = false;
     bool latexMath = true;
+    bool texMathBackslashDelimiters = false;
     bool permissiveAutolinks = true;
 };
 
@@ -26,4 +27,3 @@ private:
 };
 
 } // namespace Markdown
-
diff --git a/node_modules/react-native-enriched-markdown/cpp/wasm/build.sh b/node_modules/react-native-enriched-markdown/cpp/wasm/build.sh
index e60ff0d..0a14c68 100755
--- a/node_modules/react-native-enriched-markdown/cpp/wasm/build.sh
+++ b/node_modules/react-native-enriched-markdown/cpp/wasm/build.sh
@@ -42,13 +42,22 @@ emcc \
   -Wswitch \
   -s WASM=1 \
   -s SINGLE_FILE=1 \
-  -s EXPORTED_FUNCTIONS='["_parseMarkdown"]' \
-  -s EXPORTED_RUNTIME_METHODS='["ccall","cwrap","UTF8ToString"]' \
+  -s SINGLE_FILE_BINARY_ENCODE=0 \
+  -s STACK_SIZE=8MB \
+  -s ALLOW_MEMORY_GROWTH=1 \
+  -s EXPORTED_FUNCTIONS='["_parseMarkdown","_malloc","_free"]' \
+  -s EXPORTED_RUNTIME_METHODS='["ccall","cwrap","UTF8ToString","stringToUTF8","lengthBytesUTF8"]' \
   -s ENVIRONMENT='web' \
   -s MODULARIZE=1 \
+  -s EXPORT_ES6=1 \
   -s EXPORT_NAME='createMd4cModule' \
   -o "$OUT_DIR/md4c.js"
 
+# Metro transforms the ES module into its own module wrapper before execution.
+# SINGLE_FILE=1 means md4c.js never needs to resolve its script URL, so remove
+# import.meta.url to keep the generated parser parseable inside that wrapper.
+perl -0pi -e 's/var _scriptName=import\.meta\.url;/var _scriptName="";/' "$OUT_DIR/md4c.js"
+
 rm "$OUT_DIR/md4c.o"
 
 echo "Done → $OUT_DIR/md4c.js"
diff --git a/node_modules/react-native-enriched-markdown/cpp/wasm/md4c_wasm.cpp b/node_modules/react-native-enriched-markdown/cpp/wasm/md4c_wasm.cpp
index 7c40b40..3084d0c 100644
--- a/node_modules/react-native-enriched-markdown/cpp/wasm/md4c_wasm.cpp
+++ b/node_modules/react-native-enriched-markdown/cpp/wasm/md4c_wasm.cpp
@@ -15,9 +15,10 @@ extern "C" {
  * @param markdown   Null-terminated UTF-8 markdown input.
  * @param underline  1 → enable __ underline extension; 0 → __ means emphasis.
  * @param latexMath  1 → enable $…$ / $$…$$ LaTeX math spans; 0 → disable.
+ * @param texMathBackslashDelimiters 1 → enable \(…\) / standalone \[…\] math spans; 0 → disable.
  * @return           Null-terminated UTF-8 JSON string, valid until the next call.
  */
-const char *parseMarkdown(const char *markdown, int underline, int latexMath) {
+const char *parseMarkdown(const char *markdown, int underline, int latexMath, int texMathBackslashDelimiters) {
   if (!markdown) {
     g_resultBuffer = "{\"type\":\"Document\"}";
     return g_resultBuffer.c_str();
@@ -26,6 +27,7 @@ const char *parseMarkdown(const char *markdown, int underline, int latexMath) {
   Markdown::Md4cFlags flags;
   flags.underline = (underline != 0);
   flags.latexMath = (latexMath != 0);
+  flags.texMathBackslashDelimiters = (texMathBackslashDelimiters != 0);
 
   Markdown::MD4CParser parser;
   auto root = parser.parse(std::string(markdown), flags);
diff --git a/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdown.mm b/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdown.mm
index 9227b78..5b19c1b 100644
--- a/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdown.mm
+++ b/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdown.mm
@@ -637,6 +637,10 @@ - (void)updateProps:(Props::Shared const &)props oldProps:(Props::Shared const &
     _md4cFlags.latexMath = newViewProps.md4cFlags.latexMath;
     md4cFlagsChanged = YES;
   }
+  if (newViewProps.md4cFlags.texMathBackslashDelimiters != oldViewProps.md4cFlags.texMathBackslashDelimiters) {
+    _md4cFlags.texMathBackslashDelimiters = newViewProps.md4cFlags.texMathBackslashDelimiters;
+    md4cFlagsChanged = YES;
+  }
   BOOL markdownChanged = oldViewProps.markdown != newViewProps.markdown;
   BOOL allowTrailingMarginChanged = newViewProps.allowTrailingMargin != oldViewProps.allowTrailingMargin;
 
diff --git a/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdownText.mm b/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdownText.mm
index 239e435..d74e7a3 100644
--- a/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdownText.mm
+++ b/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdownText.mm
@@ -444,6 +444,10 @@ - (void)updateProps:(Props::Shared const &)props oldProps:(Props::Shared const &
     _md4cFlags.latexMath = newViewProps.md4cFlags.latexMath;
     md4cFlagsChanged = YES;
   }
+  if (newViewProps.md4cFlags.texMathBackslashDelimiters != oldViewProps.md4cFlags.texMathBackslashDelimiters) {
+    _md4cFlags.texMathBackslashDelimiters = newViewProps.md4cFlags.texMathBackslashDelimiters;
+    md4cFlagsChanged = YES;
+  }
   BOOL markdownChanged = oldViewProps.markdown != newViewProps.markdown;
   BOOL allowTrailingMarginChanged = newViewProps.allowTrailingMargin != oldViewProps.allowTrailingMargin;
 
diff --git a/node_modules/react-native-enriched-markdown/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/Props.h b/node_modules/react-native-enriched-markdown/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/Props.h
index c9e1984..4f75d3a 100644
--- a/node_modules/react-native-enriched-markdown/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/Props.h
+++ b/node_modules/react-native-enriched-markdown/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/Props.h
@@ -1857,6 +1857,7 @@ static inline folly::dynamic toDynamic(const EnrichedMarkdownMarkdownStyleStruct
 struct EnrichedMarkdownMd4cFlagsStruct {
   bool underline{false};
   bool latexMath{false};
+  bool texMathBackslashDelimiters{false};
 
 
 #ifdef RN_SERIALIZABLE_STATE
@@ -1866,6 +1867,7 @@ struct EnrichedMarkdownMd4cFlagsStruct {
     folly::dynamic result = folly::dynamic::object();
     result["underline"] = underline;
     result["latexMath"] = latexMath;
+    result["texMathBackslashDelimiters"] = texMathBackslashDelimiters;
     return result;
   }
 #endif
@@ -1882,6 +1884,10 @@ static inline void fromRawValue(const PropsParserContext& context, const RawValu
   if (tmp_latexMath != map.end()) {
     fromRawValue(context, tmp_latexMath->second, result.latexMath);
   }
+  auto tmp_texMathBackslashDelimiters = map.find("texMathBackslashDelimiters");
+  if (tmp_texMathBackslashDelimiters != map.end()) {
+    fromRawValue(context, tmp_texMathBackslashDelimiters->second, result.texMathBackslashDelimiters);
+  }
 }
 
 static inline std::string toString(const EnrichedMarkdownMd4cFlagsStruct &value) {
@@ -4153,6 +4159,7 @@ static inline folly::dynamic toDynamic(const EnrichedMarkdownTextMarkdownStyleSt
 struct EnrichedMarkdownTextMd4cFlagsStruct {
   bool underline{false};
   bool latexMath{false};
+  bool texMathBackslashDelimiters{false};
 
 
 #ifdef RN_SERIALIZABLE_STATE
@@ -4162,6 +4169,7 @@ struct EnrichedMarkdownTextMd4cFlagsStruct {
     folly::dynamic result = folly::dynamic::object();
     result["underline"] = underline;
     result["latexMath"] = latexMath;
+    result["texMathBackslashDelimiters"] = texMathBackslashDelimiters;
     return result;
   }
 #endif
@@ -4178,6 +4186,10 @@ static inline void fromRawValue(const PropsParserContext& context, const RawValu
   if (tmp_latexMath != map.end()) {
     fromRawValue(context, tmp_latexMath->second, result.latexMath);
   }
+  auto tmp_texMathBackslashDelimiters = map.find("texMathBackslashDelimiters");
+  if (tmp_texMathBackslashDelimiters != map.end()) {
+    fromRawValue(context, tmp_texMathBackslashDelimiters->second, result.texMathBackslashDelimiters);
+  }
 }
 
 static inline std::string toString(const EnrichedMarkdownTextMd4cFlagsStruct &value) {
diff --git a/node_modules/react-native-enriched-markdown/ios/internals/MeasurementCache.h b/node_modules/react-native-enriched-markdown/ios/internals/MeasurementCache.h
index e669d6c..46bde8c 100644
--- a/node_modules/react-native-enriched-markdown/ios/internals/MeasurementCache.h
+++ b/node_modules/react-native-enriched-markdown/ios/internals/MeasurementCache.h
@@ -37,6 +37,7 @@ struct MeasurementCacheKey {
   double maxFontSizeMultiplier;
   bool md4cFlagsUnderline;
   bool md4cFlagsLatexMath;
+  bool md4cFlagsTexMathBackslashDelimiters;
   size_t styleFingerprint;
   CGFloat fontScale;
   MarkdownFlavor flavor;
@@ -44,9 +45,10 @@ struct MeasurementCacheKey {
   bool operator==(const MeasurementCacheKey &other) const
   {
     return std::tie(markdown, maxWidth, allowTrailingMargin, allowFontScaling, maxFontSizeMultiplier,
-                    md4cFlagsUnderline, md4cFlagsLatexMath, styleFingerprint, fontScale, flavor) ==
+                    md4cFlagsUnderline, md4cFlagsLatexMath, md4cFlagsTexMathBackslashDelimiters, styleFingerprint, fontScale, flavor) ==
            std::tie(other.markdown, other.maxWidth, other.allowTrailingMargin, other.allowFontScaling,
                     other.maxFontSizeMultiplier, other.md4cFlagsUnderline, other.md4cFlagsLatexMath,
+                    other.md4cFlagsTexMathBackslashDelimiters,
                     other.styleFingerprint, other.fontScale, other.flavor);
   }
 };
@@ -62,6 +64,7 @@ struct MeasurementCacheKeyHash {
     HashUtils::hash_one(h, key.maxFontSizeMultiplier);
     HashUtils::hash_one(h, key.md4cFlagsUnderline);
     HashUtils::hash_one(h, key.md4cFlagsLatexMath);
+    HashUtils::hash_one(h, key.md4cFlagsTexMathBackslashDelimiters);
     HashUtils::hash_one(h, key.styleFingerprint);
     HashUtils::hash_one(h, key.fontScale);
     HashUtils::hash_one(h, static_cast<uint8_t>(key.flavor));
@@ -139,6 +142,7 @@ inline MeasurementCacheKey buildMeasurementCacheKey(const PropsType &props, CGFl
       .maxFontSizeMultiplier = props.maxFontSizeMultiplier,
       .md4cFlagsUnderline = props.md4cFlags.underline,
       .md4cFlagsLatexMath = props.md4cFlags.latexMath,
+      .md4cFlagsTexMathBackslashDelimiters = props.md4cFlags.texMathBackslashDelimiters,
       .styleFingerprint = computeStyleFingerprint(props.markdownStyle),
       .fontScale = fontScale,
       .flavor = flavor,
diff --git a/node_modules/react-native-enriched-markdown/ios/parser/ENRMMarkdownParser.h b/node_modules/react-native-enriched-markdown/ios/parser/ENRMMarkdownParser.h
index 8844fe3..01b85df 100644
--- a/node_modules/react-native-enriched-markdown/ios/parser/ENRMMarkdownParser.h
+++ b/node_modules/react-native-enriched-markdown/ios/parser/ENRMMarkdownParser.h
@@ -5,6 +5,7 @@
 
 @property (nonatomic, assign) BOOL underline;
 @property (nonatomic, assign) BOOL latexMath;
+@property (nonatomic, assign) BOOL texMathBackslashDelimiters;
 
 + (instancetype)defaultFlags;
 
diff --git a/node_modules/react-native-enriched-markdown/ios/parser/ENRMMarkdownParser.mm b/node_modules/react-native-enriched-markdown/ios/parser/ENRMMarkdownParser.mm
index 558f29f..4b1d822 100644
--- a/node_modules/react-native-enriched-markdown/ios/parser/ENRMMarkdownParser.mm
+++ b/node_modules/react-native-enriched-markdown/ios/parser/ENRMMarkdownParser.mm
@@ -10,6 +10,7 @@ - (instancetype)init
   if (self = [super init]) {
     _underline = NO;
     _latexMath = YES;
+    _texMathBackslashDelimiters = NO;
   }
   return self;
 }
@@ -24,6 +25,7 @@ - (id)copyWithZone:(NSZone *)zone
   ENRMMd4cFlags *copy = [[ENRMMd4cFlags allocWithZone:zone] init];
   copy.underline = self.underline;
   copy.latexMath = self.latexMath;
+  copy.texMathBackslashDelimiters = self.texMathBackslashDelimiters;
   return copy;
 }
 
diff --git a/node_modules/react-native-enriched-markdown/ios/parser/MarkdownParserBridge.mm b/node_modules/react-native-enriched-markdown/ios/parser/MarkdownParserBridge.mm
index 0a3a40c..b32a4c4 100644
--- a/node_modules/react-native-enriched-markdown/ios/parser/MarkdownParserBridge.mm
+++ b/node_modules/react-native-enriched-markdown/ios/parser/MarkdownParserBridge.mm
@@ -140,6 +140,7 @@
   Markdown::Md4cFlags cppFlags;
   cppFlags.underline = flags.underline;
   cppFlags.latexMath = flags.latexMath;
+  cppFlags.texMathBackslashDelimiters = flags.texMathBackslashDelimiters;
 
   Markdown::MD4CParser parser;
   auto cppAST = parser.parse(cppMarkdown, cppFlags);
diff --git a/node_modules/react-native-enriched-markdown/ios/utils/ENRMTailFadeInAnimator.m b/node_modules/react-native-enriched-markdown/ios/utils/ENRMTailFadeInAnimator.m
index e3630fb..4175787 100644
--- a/node_modules/react-native-enriched-markdown/ios/utils/ENRMTailFadeInAnimator.m
+++ b/node_modules/react-native-enriched-markdown/ios/utils/ENRMTailFadeInAnimator.m
@@ -8,15 +8,24 @@
 typedef struct {
   NSRange range;
   __unsafe_unretained RCTUIColor *color;
+  CFTimeInterval startTime;
 } ENRMColorEntry;
 
+@interface ENRMActiveFadeRange : NSObject
+@property (nonatomic, assign) NSRange range;
+@property (nonatomic, assign) CFTimeInterval startTime;
+@end
+
+@implementation ENRMActiveFadeRange
+@end
+
 @implementation ENRMTailFadeInAnimator {
   __weak ENRMPlatformTextView *_textView;
 #if !TARGET_OS_OSX
   CADisplayLink *_displayLink;
 #endif
-  CFTimeInterval _startTime;
 
+  NSMutableArray<ENRMActiveFadeRange *> *_activeRanges;
   NSArray<RCTUIColor *> *_retainedColors;
   ENRMColorEntry *_colorEntries;
   NSUInteger _entriesCount;
@@ -27,6 +36,7 @@ - (instancetype)initWithTextView:(ENRMPlatformTextView *)textView
   self = [super init];
   if (self) {
     _textView = textView;
+    _activeRanges = [NSMutableArray array];
   }
   return self;
 }
@@ -41,29 +51,40 @@ - (void)dealloc
 
 - (void)animateFrom:(NSUInteger)tailStart to:(NSUInteger)tailEnd
 {
-  [self cancel];
-
   NSTextStorage *storage = _textView.textStorage;
-  if (!storage || tailEnd <= tailStart || tailEnd > storage.length)
+  if (!storage)
     return;
 
-  NSRange range = NSMakeRange(tailStart, tailEnd - tailStart);
+  CFTimeInterval now = CACurrentMediaTime();
+  [self pruneCompletedRangesAtTime:now textLength:storage.length];
 
-  [self snapshotColorsInRange:range storage:storage];
-  [self updateAlpha:0.0];
+  if (tailEnd > tailStart && tailEnd <= storage.length) {
+    ENRMActiveFadeRange *activeRange = [ENRMActiveFadeRange new];
+    activeRange.range = NSMakeRange(tailStart, tailEnd - tailStart);
+    activeRange.startTime = now;
+    [_activeRanges addObject:activeRange];
+  }
+
+  if (_activeRanges.count == 0)
+    return;
+
+  [self snapshotColorsForActiveRangesInStorage:storage];
+  [self updateAlphaAtTime:now];
 
 #if !TARGET_OS_OSX
-  _startTime = CACurrentMediaTime();
-  _displayLink = [CADisplayLink displayLinkWithTarget:self selector:@selector(step:)];
-  // 0 tells the system to use the display's maximum frame rate — 60 Hz on standard displays and 120 Hz on ProMotion ones
-  _displayLink.preferredFramesPerSecond = 0;
-  [_displayLink addToRunLoop:[NSRunLoop mainRunLoop] forMode:NSRunLoopCommonModes];
+  if (!_displayLink) {
+    _displayLink = [CADisplayLink displayLinkWithTarget:self selector:@selector(step:)];
+    // 0 tells the system to use the display's maximum frame rate — 60 Hz on standard displays and 120 Hz on ProMotion ones
+    _displayLink.preferredFramesPerSecond = 0;
+    [_displayLink addToRunLoop:[NSRunLoop mainRunLoop] forMode:NSRunLoopCommonModes];
+  }
 #else
   // TODO: Implement the tail fade-in animation on macOS.
   // CADisplayLink doesn't exist on macOS; the equivalent is CVDisplayLink (Core Video)
   // or an NSTimer driven at the display refresh rate. The iOS step:/eased-progress
   // logic below can be reused directly once a display-sync callback is wired up.
-  [self updateAlpha:1.0];
+  [self updateAlphaAtTime:now + kFadeDuration];
+  [_activeRanges removeAllObjects];
   [self cleanupEntries];
 #endif
 }
@@ -71,20 +92,21 @@ - (void)animateFrom:(NSUInteger)tailStart to:(NSUInteger)tailEnd
 #if !TARGET_OS_OSX
 - (void)step:(CADisplayLink *)link
 {
-  CFTimeInterval elapsed = CACurrentMediaTime() - _startTime;
-  CGFloat progress = fmin(elapsed / kFadeDuration, 1.0);
+  CFTimeInterval now = CACurrentMediaTime();
+  [self updateAlphaAtTime:now];
 
-  CGFloat eased = 1.0 - (1.0 - progress) * (1.0 - progress);
-
-  [self updateAlpha:eased];
+  NSTextStorage *storage = _textView.textStorage;
+  [self pruneCompletedRangesAtTime:now textLength:storage.length];
 
-  if (progress >= 1.0) {
-    [self cancel];
+  if (_activeRanges.count == 0) {
+    [_displayLink invalidate];
+    _displayLink = nil;
+    [self cleanupEntries];
   }
 }
 #endif
 
-- (void)updateAlpha:(CGFloat)alpha
+- (void)updateAlphaAtTime:(CFTimeInterval)now
 {
   NSTextStorage *storage = _textView.textStorage;
   if (!storage || _entriesCount == 0)
@@ -94,26 +116,37 @@ - (void)updateAlpha:(CGFloat)alpha
   for (NSUInteger i = 0; i < _entriesCount; i++) {
     ENRMColorEntry entry = _colorEntries[i];
     if (NSMaxRange(entry.range) <= storage.length) {
-      RCTUIColor *fadedColor = [entry.color colorWithAlphaComponent:alpha];
+      CFTimeInterval elapsed = now - entry.startTime;
+      CGFloat progress = fmin(fmax(elapsed / kFadeDuration, 0.0), 1.0);
+      CGFloat eased = 1.0 - (1.0 - progress) * (1.0 - progress);
+      RCTUIColor *fadedColor = [entry.color colorWithAlphaComponent:eased];
       [storage addAttribute:NSForegroundColorAttributeName value:fadedColor range:entry.range];
     }
   }
   [storage endEditing];
 }
 
-- (void)snapshotColorsInRange:(NSRange)range storage:(NSTextStorage *)storage
+- (void)snapshotColorsForActiveRangesInStorage:(NSTextStorage *)storage
 {
   [self cleanupEntries];
 
   NSMutableArray<RCTUIColor *> *colors = [NSMutableArray array];
   NSMutableArray<NSValue *> *ranges = [NSMutableArray array];
-  [storage enumerateAttribute:NSForegroundColorAttributeName
-                      inRange:range
-                      options:0
-                   usingBlock:^(RCTUIColor *color, NSRange subRange, BOOL *stop) {
-                     [colors addObject:color ?: [RCTUIColor labelColor]];
-                     [ranges addObject:[NSValue valueWithRange:subRange]];
-                   }];
+  NSMutableArray<NSNumber *> *startTimes = [NSMutableArray array];
+
+  for (ENRMActiveFadeRange *activeRange in _activeRanges) {
+    if (NSMaxRange(activeRange.range) > storage.length)
+      continue;
+
+    [storage enumerateAttribute:NSForegroundColorAttributeName
+                        inRange:activeRange.range
+                        options:0
+                     usingBlock:^(RCTUIColor *color, NSRange subRange, BOOL *stop) {
+                       [colors addObject:color ?: [RCTUIColor labelColor]];
+                       [ranges addObject:[NSValue valueWithRange:subRange]];
+                       [startTimes addObject:@(activeRange.startTime)];
+                     }];
+  }
 
   _entriesCount = colors.count;
   _retainedColors = [colors copy];
@@ -122,6 +155,22 @@ - (void)snapshotColorsInRange:(NSRange)range storage:(NSTextStorage *)storage
   for (NSUInteger i = 0; i < _entriesCount; i++) {
     _colorEntries[i].color = _retainedColors[i];
     _colorEntries[i].range = [ranges[i] rangeValue];
+    _colorEntries[i].startTime = [startTimes[i] doubleValue];
+  }
+}
+
+- (void)pruneCompletedRangesAtTime:(CFTimeInterval)now textLength:(NSUInteger)textLength
+{
+  NSIndexSet *completed = [_activeRanges indexesOfObjectsPassingTest:^BOOL(ENRMActiveFadeRange *range, NSUInteger idx, BOOL *stop) {
+    return NSMaxRange(range.range) > textLength || now - range.startTime >= kFadeDuration;
+  }];
+
+  if (completed.count > 0) {
+    [_activeRanges removeObjectsAtIndexes:completed];
+    NSTextStorage *storage = _textView.textStorage;
+    if (storage) {
+      [self snapshotColorsForActiveRangesInStorage:storage];
+    }
   }
 }
 
@@ -133,9 +182,11 @@ - (void)cancel
 #endif
 
   if (_entriesCount > 0) {
-    [self updateAlpha:1.0];
+    [self updateAlphaAtTime:CACurrentMediaTime() + kFadeDuration];
     [self cleanupEntries];
   }
+
+  [_activeRanges removeAllObjects];
 }
 
 - (void)cleanupEntries
diff --git a/node_modules/react-native-enriched-markdown/lib/module/EnrichedMarkdownNativeComponent.ts b/node_modules/react-native-enriched-markdown/lib/module/EnrichedMarkdownNativeComponent.ts
index 06024b4..9353921 100644
--- a/node_modules/react-native-enriched-markdown/lib/module/EnrichedMarkdownNativeComponent.ts
+++ b/node_modules/react-native-enriched-markdown/lib/module/EnrichedMarkdownNativeComponent.ts
@@ -224,6 +224,7 @@ export interface Md4cFlagsInternal {
    * @default true
    */
   latexMath: boolean;
+  texMathBackslashDelimiters: boolean;
 }
 
 export interface NativeProps extends ViewProps {
diff --git a/node_modules/react-native-enriched-markdown/lib/module/EnrichedMarkdownTextNativeComponent.ts b/node_modules/react-native-enriched-markdown/lib/module/EnrichedMarkdownTextNativeComponent.ts
index e238ecf..e3639f6 100644
--- a/node_modules/react-native-enriched-markdown/lib/module/EnrichedMarkdownTextNativeComponent.ts
+++ b/node_modules/react-native-enriched-markdown/lib/module/EnrichedMarkdownTextNativeComponent.ts
@@ -224,6 +224,7 @@ export interface Md4cFlagsInternal {
    * @default true
    */
   latexMath: boolean;
+  texMathBackslashDelimiters: boolean;
 }
 
 export interface NativeProps extends ViewProps {
diff --git a/node_modules/react-native-enriched-markdown/lib/module/index.web.js b/node_modules/react-native-enriched-markdown/lib/module/index.web.js
index d8308e6..98e32e6 100644
--- a/node_modules/react-native-enriched-markdown/lib/module/index.web.js
+++ b/node_modules/react-native-enriched-markdown/lib/module/index.web.js
@@ -1,4 +1,5 @@
 "use strict";
 
 export { EnrichedMarkdownText, default } from "./web/EnrichedMarkdownText.js";
+export { preloadMarkdownRuntime } from "./web/parseMarkdown.js";
 //# sourceMappingURL=index.web.js.map
\ No newline at end of file
diff --git a/node_modules/react-native-enriched-markdown/lib/module/native/EnrichedMarkdownText.js b/node_modules/react-native-enriched-markdown/lib/module/native/EnrichedMarkdownText.js
index 490b567..d25e727 100644
--- a/node_modules/react-native-enriched-markdown/lib/module/native/EnrichedMarkdownText.js
+++ b/node_modules/react-native-enriched-markdown/lib/module/native/EnrichedMarkdownText.js
@@ -7,7 +7,8 @@ import { normalizeMarkdownStyle } from '../normalizeMarkdownStyle';
 import { jsx as _jsx } from "react/jsx-runtime";
 const defaultMd4cFlags = {
   underline: false,
-  latexMath: true
+  latexMath: true,
+  texMathBackslashDelimiters: false
 };
 export const EnrichedMarkdownText = ({
   markdown,
@@ -38,7 +39,8 @@ export const EnrichedMarkdownText = ({
   const normalizedStyle = normalizedStyleRef.current;
   const normalizedMd4cFlags = useMemo(() => ({
     underline: md4cFlags.underline ?? false,
-    latexMath: md4cFlags.latexMath ?? true
+    latexMath: md4cFlags.latexMath ?? true,
+    texMathBackslashDelimiters: md4cFlags.texMathBackslashDelimiters ?? false
   }), [md4cFlags]);
   const contextMenuCallbacksRef = useRef(new Map());
   useEffect(() => {
diff --git a/node_modules/react-native-enriched-markdown/lib/module/web/EnrichedMarkdownText.js b/node_modules/react-native-enriched-markdown/lib/module/web/EnrichedMarkdownText.js
index c6c73b0..aa651fe 100644
--- a/node_modules/react-native-enriched-markdown/lib/module/web/EnrichedMarkdownText.js
+++ b/node_modules/react-native-enriched-markdown/lib/module/web/EnrichedMarkdownText.js
@@ -1,13 +1,22 @@
 "use strict";
 
-import { useState, useEffect, useMemo } from 'react';
+import { useState, useEffect, useLayoutEffect, useMemo, useRef } from 'react';
 import { normalizeMarkdownStyle } from "../normalizeMarkdownStyle.web.js";
-import { zeroTrailingMargins, parseErrorFallbackStyle, buildStyles } from "./styles.js";
-import { parseMarkdown } from "./parseMarkdown.js";
+import { zeroTrailingMargins, buildStyles } from "./styles.js";
+import { parseMarkdown, parseMarkdownSyncIfReady } from "./parseMarkdown.js";
 import { RenderNode } from "./renderers/index.js";
 import { indexTaskItems, markInlineImages } from "./utils.js";
 import { loadKaTeX } from "./katex.js";
+import { createStreamingRevealState, markStreamingRevealOffsets, readRenderedComparisonText, updateStreamingRevealRanges } from "./streamingReveal.js";
 import { jsx as _jsx } from "react/jsx-runtime";
+const STREAMING_REVEAL_RANGE_TTL_MS = 260;
+const STREAMING_REVEAL_CLEANUP_BUFFER_MS = 16;
+function prepareAstForRendering(ast) {
+  indexTaskItems(ast);
+  markInlineImages(ast);
+  markStreamingRevealOffsets(ast);
+  return ast;
+}
 export const EnrichedMarkdownText = ({
   markdown,
   markdownStyle = {},
@@ -18,30 +27,41 @@ export const EnrichedMarkdownText = ({
   allowTrailingMargin = false,
   containerStyle,
   selectable = true,
+  streamingAnimation = false,
+  renderRawFallback = true,
   dir,
   ...rest
 }) => {
   const normalizedStyle = useMemo(() => normalizeMarkdownStyle(markdownStyle), [markdownStyle]);
-  const [ast, setAst] = useState(null);
-  const [katex, setKatex] = useState(null);
-  const [parseError, setParseError] = useState(false);
   const {
     underline = false,
-    latexMath = true
+    latexMath = true,
+    texMathBackslashDelimiters = false
   } = md4cFlags;
+  const syncAst = useMemo(() => {
+    const parsed = parseMarkdownSyncIfReady(markdown, {
+      underline,
+      latexMath,
+      texMathBackslashDelimiters
+    });
+    return parsed ? prepareAstForRendering(parsed) : null;
+  }, [markdown, underline, latexMath, texMathBackslashDelimiters]);
+  const [ast, setAst] = useState(null);
+  const [katex, setKatex] = useState(null);
+  const [parseError, setParseError] = useState(false);
   useEffect(() => {
+    // A warm synchronous AST is already the rendered authority. Only cold or
+    // failed synchronous parsing needs an asynchronous result publication.
+    if (syncAst) return;
     let cancelled = false;
-    const katexPromise = latexMath ? loadKaTeX() : Promise.resolve(null);
-    Promise.all([parseMarkdown(markdown, {
+    parseMarkdown(markdown, {
       underline,
-      latexMath
-    }), katexPromise]).then(([result, katexInstance]) => {
+      latexMath,
+      texMathBackslashDelimiters
+    }).then(result => {
       if (!cancelled) {
-        indexTaskItems(result);
-        markInlineImages(result);
         setParseError(false);
-        setKatex(katexInstance);
-        setAst(result);
+        setAst(prepareAstForRendering(result));
       }
     }).catch(error => {
       if (!cancelled) {
@@ -50,21 +70,84 @@ export const EnrichedMarkdownText = ({
         }
         setParseError(true);
         setAst(null);
-        setKatex(null);
       }
     });
     return () => {
       cancelled = true;
     };
-  }, [markdown, underline, latexMath]);
+  }, [syncAst, markdown, underline, latexMath, texMathBackslashDelimiters]);
+  useEffect(() => {
+    let cancelled = false;
+    if (!latexMath) {
+      setKatex(null);
+      return;
+    }
+    // Optional math readiness is independent of document parsing and streaming.
+    void loadKaTeX().then(instance => {
+      if (!cancelled) setKatex(instance);
+    });
+    return () => { cancelled = true; };
+  }, [latexMath]);
+  const renderedAst = syncAst ?? ast;
   const callbacks = useMemo(() => ({
     onLinkPress,
     onLinkLongPress,
     onTaskListItemPress
   }), [onLinkPress, onLinkLongPress, onTaskListItemPress]);
+  const previousComparisonTextRef = useRef('');
+  const previousSourceLengthRef = useRef(0);
+  const activeRevealRangesRef = useRef([]);
+  const revealCleanupTimerRef = useRef(null);
+  const [revealCleanupRevision, setRevealCleanupRevision] = useState(0);
+  const currentComparisonText = useMemo(() => readRenderedComparisonText(renderedAst), [renderedAst]);
+  const activeRevealRanges = useMemo(() => streamingAnimation ? updateStreamingRevealRanges({
+    activeRanges: activeRevealRangesRef.current,
+    previousComparisonText: previousComparisonTextRef.current,
+    currentComparisonText,
+    nowMs: Date.now(),
+    ttlMs: STREAMING_REVEAL_RANGE_TTL_MS,
+    previousSourceLength: previousSourceLengthRef.current,
+    currentSourceLength: markdown.length
+  }) : [], [currentComparisonText, markdown, revealCleanupRevision, streamingAnimation]);
+  const streamingReveal = createStreamingRevealState({
+    enabled: streamingAnimation,
+    activeRanges: activeRevealRanges
+  });
+  useLayoutEffect(() => {
+    if (!streamingAnimation) {
+      activeRevealRangesRef.current = [];
+      previousComparisonTextRef.current = currentComparisonText;
+      previousSourceLengthRef.current = markdown.length;
+      return;
+    }
+    activeRevealRangesRef.current = activeRevealRanges;
+    previousComparisonTextRef.current = currentComparisonText;
+    previousSourceLengthRef.current = markdown.length;
+  }, [activeRevealRanges, currentComparisonText, markdown, streamingAnimation]);
+  useEffect(() => {
+    if (revealCleanupTimerRef.current) {
+      clearTimeout(revealCleanupTimerRef.current);
+      revealCleanupTimerRef.current = null;
+    }
+    if (!streamingAnimation || activeRevealRanges.length === 0) return;
+    const nowMs = Date.now();
+    const nextExpiryMs = Math.min(...activeRevealRanges.map(range => range.expiresAtMs));
+    const delayMs = Math.max(0, nextExpiryMs - nowMs + STREAMING_REVEAL_CLEANUP_BUFFER_MS);
+    revealCleanupTimerRef.current = setTimeout(() => {
+      revealCleanupTimerRef.current = null;
+      setRevealCleanupRevision(revision => revision + 1);
+    }, delayMs);
+    return () => {
+      if (revealCleanupTimerRef.current) {
+        clearTimeout(revealCleanupTimerRef.current);
+        revealCleanupTimerRef.current = null;
+      }
+    };
+  }, [activeRevealRanges, streamingAnimation]);
   const capabilities = useMemo(() => ({
-    katex
-  }), [katex]);
+    katex,
+    streamingReveal
+  }), [katex, streamingReveal]);
   const lastChildStyle = useMemo(() => allowTrailingMargin ? normalizedStyle : zeroTrailingMargins(normalizedStyle), [normalizedStyle, allowTrailingMargin]);
   const styles = useMemo(() => buildStyles(normalizedStyle), [normalizedStyle]);
   const lastChildStyles = useMemo(() => buildStyles(lastChildStyle), [lastChildStyle]);
@@ -76,19 +159,46 @@ export const EnrichedMarkdownText = ({
       userSelect: 'none'
     })
   }), [containerStyle, selectable]);
-  if (parseError) {
+  if (parseError && !syncAst) {
+    return /*#__PURE__*/_jsx("div", {
+      style: wrapperStyle,
+      dir: dir,
+      ...rest,
+      children: /*#__PURE__*/_jsx("p", {
+        style: {
+          ...lastChildStyles.paragraph,
+          whiteSpace: 'pre-wrap'
+        },
+        children: markdown
+      })
+    });
+  }
+  if (!renderedAst) {
+    const fallbackParagraphStyle = {
+      ...lastChildStyles.paragraph,
+      whiteSpace: 'pre-wrap',
+      ...(renderRawFallback === 'hidden' ? {
+        visibility: 'hidden'
+      } : undefined)
+    };
+    if (!renderRawFallback) {
+      return /*#__PURE__*/_jsx("div", {
+        style: wrapperStyle,
+        dir: dir,
+        ...rest
+      });
+    }
     return /*#__PURE__*/_jsx("div", {
       style: wrapperStyle,
       dir: dir,
       ...rest,
-      children: /*#__PURE__*/_jsx("pre", {
-        style: parseErrorFallbackStyle,
+      children: /*#__PURE__*/_jsx("p", {
+        style: fallbackParagraphStyle,
         children: markdown
       })
     });
   }
-  if (!ast) return null;
-  const children = ast.children ?? [];
+  const children = renderedAst.children ?? [];
   const lastIdx = children.length - 1;
   return /*#__PURE__*/_jsx("div", {
     style: wrapperStyle,
diff --git a/node_modules/react-native-enriched-markdown/lib/module/web/parseMarkdown.js b/node_modules/react-native-enriched-markdown/lib/module/web/parseMarkdown.js
index 7604c13..20039e4 100644
--- a/node_modules/react-native-enriched-markdown/lib/module/web/parseMarkdown.js
+++ b/node_modules/react-native-enriched-markdown/lib/module/web/parseMarkdown.js
@@ -1,32 +1,169 @@
 "use strict";
 
+import createMd4cModule from './wasm/md4c.js';
 // Caching the Promise (not the resolved value) means concurrent callers share
 // a single WASM initialization — no duplicate loading.
 let parserPromise = null;
+let parserRuntime = null;
+const PARSE_CACHE_MAX_ENTRIES = 48;
+const PARSE_CACHE_MAX_MARKDOWN_CHARS = 32000;
+const parseCache = new Map();
+const parseResultCache = new Map();
+const WASM_FACTORY_UNAVAILABLE_MESSAGE = 'WASM parser module did not export a factory';
+const WASM_INPUT_ALLOCATION_FAILED_MESSAGE = 'WASM parser failed to allocate markdown input';
+function resolveMd4cModuleFactory(value) {
+  if (typeof value === 'function') {
+    return value;
+  }
+  if (typeof value !== 'object' || value === null || !('default' in value)) {
+    return null;
+  }
+  return resolveMd4cModuleFactory(value.default);
+}
 
 // SINGLE_FILE=1 inlines the WASM binary as base64 inside md4c.js, so no
-// network fetch is needed — only a one-time decode + compile on first call.
+// runtime fetch is needed — only a one-time decode + compile on first call.
 function initializeParser() {
   if (!parserPromise) {
-    parserPromise = import('./wasm/md4c').then(module => module.default()).then(wasmModule => wasmModule.cwrap('parseMarkdown', 'string', ['string', 'number', 'number'])).catch(error => {
+    parserPromise = Promise.resolve().then(() => {
+      const factory = resolveMd4cModuleFactory(createMd4cModule);
+      if (!factory) {
+        throw new Error(WASM_FACTORY_UNAVAILABLE_MESSAGE);
+      }
+      return factory();
+    }).then(wasmModule => ({
+      parse: wasmModule.cwrap('parseMarkdown', 'string', ['number', 'number', 'number', 'number']),
+      wasmModule
+    })).then(runtime => {
+      parserRuntime = runtime;
+      return runtime;
+    }).catch(error => {
       parserPromise = null;
+      parserRuntime = null;
       throw error;
     });
   }
   return parserPromise;
 }
+export async function preloadMarkdownRuntime() {
+  try {
+    await initializeParser();
+  } catch (error) {
+    if (error instanceof Error && error.message === WASM_FACTORY_UNAVAILABLE_MESSAGE && typeof window === 'undefined') {
+      return;
+    }
+    throw error;
+  }
+}
 function isASTNode(value) {
   return typeof value === 'object' && value !== null && 'type' in value && typeof value.type === 'string';
 }
+function shouldCacheParseResult(markdown) {
+  return markdown.length <= PARSE_CACHE_MAX_MARKDOWN_CHARS;
+}
+function buildCacheKey(markdown, underline, latexMath, texMathBackslashDelimiters) {
+  return shouldCacheParseResult(markdown) ? `${underline ? 1 : 0}\0${latexMath ? 1 : 0}\0${texMathBackslashDelimiters ? 1 : 0}\0${markdown}` : null;
+}
+function rememberParsedResult(cacheKey, ast) {
+  if (!cacheKey) return;
+  parseResultCache.set(cacheKey, cloneAst(ast));
+  parseCache.delete(cacheKey);
+  while (parseResultCache.size > PARSE_CACHE_MAX_ENTRIES) {
+    const oldestKey = parseResultCache.keys().next().value;
+    if (typeof oldestKey !== 'string') break;
+    parseResultCache.delete(oldestKey);
+  }
+}
+function readCachedParsedResult(cacheKey) {
+  if (!cacheKey) return null;
+  const cached = parseResultCache.get(cacheKey);
+  if (!cached) return null;
+  parseResultCache.delete(cacheKey);
+  parseResultCache.set(cacheKey, cached);
+  return cloneAst(cached);
+}
+function parseMarkdownWithHeapInput(runtime, markdown, underline, latexMath, texMathBackslashDelimiters) {
+  const {
+    wasmModule
+  } = runtime;
+  const inputBytes = wasmModule.lengthBytesUTF8(markdown) + 1;
+  const inputPointer = wasmModule._malloc(inputBytes);
+  if (!inputPointer) {
+    throw new Error(WASM_INPUT_ALLOCATION_FAILED_MESSAGE);
+  }
+  try {
+    wasmModule.stringToUTF8(markdown, inputPointer, inputBytes);
+    return runtime.parse(inputPointer, underline ? 1 : 0, latexMath ? 1 : 0, texMathBackslashDelimiters ? 1 : 0);
+  } finally {
+    wasmModule._free(inputPointer);
+  }
+}
 export async function parseMarkdown(markdown, {
   underline = false,
-  latexMath = true
+  latexMath = true,
+  texMathBackslashDelimiters = false
+} = {}) {
+  const cacheKey = buildCacheKey(markdown, underline, latexMath, texMathBackslashDelimiters);
+  const cachedResult = readCachedParsedResult(cacheKey);
+  if (cachedResult) return cachedResult;
+  if (cacheKey) {
+    const cached = parseCache.get(cacheKey);
+    if (cached) {
+      parseCache.delete(cacheKey);
+      parseCache.set(cacheKey, cached);
+      return cloneAst(await cached);
+    }
+  }
+  const parsed = initializeParser().then(runtime => {
+    const result = JSON.parse(parseMarkdownWithHeapInput(runtime, markdown, underline, latexMath, texMathBackslashDelimiters));
+    if (!isASTNode(result)) {
+      throw new Error('WASM parser returned invalid AST');
+    }
+    rememberParsedResult(cacheKey, result);
+    return result;
+  });
+  if (cacheKey) {
+    parseCache.set(cacheKey, parsed);
+    while (parseCache.size > PARSE_CACHE_MAX_ENTRIES) {
+      const oldestKey = parseCache.keys().next().value;
+      if (typeof oldestKey !== 'string') break;
+      parseCache.delete(oldestKey);
+    }
+  }
+  try {
+    return cloneAst(await parsed);
+  } catch (error) {
+    if (cacheKey) {
+      parseCache.delete(cacheKey);
+    }
+    throw error;
+  }
+}
+export function parseMarkdownSyncIfReady(markdown, {
+  underline = false,
+  latexMath = true,
+  texMathBackslashDelimiters = false
 } = {}) {
-  const parse = await initializeParser();
-  const result = JSON.parse(parse(markdown, underline ? 1 : 0, latexMath ? 1 : 0));
-  if (!isASTNode(result)) {
-    throw new Error('WASM parser returned invalid AST');
+  const cacheKey = buildCacheKey(markdown, underline, latexMath, texMathBackslashDelimiters);
+  const cachedResult = readCachedParsedResult(cacheKey);
+  if (cachedResult) return cachedResult;
+  if (!parserRuntime) return null;
+  try {
+    const result = JSON.parse(parseMarkdownWithHeapInput(parserRuntime, markdown, underline, latexMath, texMathBackslashDelimiters));
+    if (!isASTNode(result)) {
+      throw new Error('WASM parser returned invalid AST');
+    }
+    rememberParsedResult(cacheKey, result);
+    return cloneAst(result);
+  } catch (error) {
+    if (cacheKey) {
+      parseCache.delete(cacheKey);
+      parseResultCache.delete(cacheKey);
+    }
+    return null;
   }
-  return result;
+}
+function cloneAst(ast) {
+  return JSON.parse(JSON.stringify(ast));
 }
 //# sourceMappingURL=parseMarkdown.js.map
\ No newline at end of file
diff --git a/node_modules/react-native-enriched-markdown/lib/module/web/renderers/BlockRenderers.js b/node_modules/react-native-enriched-markdown/lib/module/web/renderers/BlockRenderers.js
index 5bdaee9..ab8bc37 100644
--- a/node_modules/react-native-enriched-markdown/lib/module/web/renderers/BlockRenderers.js
+++ b/node_modules/react-native-enriched-markdown/lib/module/web/renderers/BlockRenderers.js
@@ -8,6 +8,9 @@ function ParagraphRenderer({
   node,
   styles,
   parentType,
+  parentAttributes,
+  childIndex,
+  siblingCount,
   renderChildren
 }) {
   const isImageOnly = node.children?.length === 1 && node.children[0]?.type === 'Image';
@@ -21,7 +24,15 @@ function ParagraphRenderer({
     });
   }
   if (parentType === 'ListItem') {
-    return /*#__PURE__*/_jsx("span", {
+    if (parentAttributes?.isTask === 'true') {
+      return /*#__PURE__*/_jsx("span", {
+        children: renderChildren(node)
+      });
+    }
+    const isLastListItemChild = childIndex !== undefined && siblingCount !== undefined ? childIndex >= siblingCount - 1 : false;
+    const paragraphStyle = isLastListItemChild ? styles.paragraphInLastListItemChild : styles.paragraphInListItem;
+    return /*#__PURE__*/_jsx("p", {
+      style: paragraphStyle,
       children: renderChildren(node)
     });
   }
diff --git a/node_modules/react-native-enriched-markdown/lib/module/web/renderers/InlineRenderers.js b/node_modules/react-native-enriched-markdown/lib/module/web/renderers/InlineRenderers.js
index 7c31c2b..5bc65a7 100644
--- a/node_modules/react-native-enriched-markdown/lib/module/web/renderers/InlineRenderers.js
+++ b/node_modules/react-native-enriched-markdown/lib/module/web/renderers/InlineRenderers.js
@@ -2,12 +2,20 @@
 
 import { extractNodeText } from "../utils.js";
 import { KaTeXRenderer } from "./KaTeXRenderer.js";
+import { renderStreamingRevealText, shouldRevealTextForParent } from "../streamingReveal.js";
 import { Fragment as _Fragment, jsx as _jsx } from "react/jsx-runtime";
 function TextRenderer({
-  node
+  node,
+  parentType,
+  capabilities
 }) {
   return /*#__PURE__*/_jsx(_Fragment, {
-    children: node.content ?? ''
+    children: renderStreamingRevealText({
+      capabilities,
+      text: node.content ?? '',
+      revealable: shouldRevealTextForParent(parentType),
+      startOffset: node.streamingRevealStartOffset ?? 0
+    })
   });
 }
 function LineBreakRenderer(_props) {
@@ -58,9 +66,15 @@ function CodeRenderer({
   styles,
   renderChildren
 }) {
+  if (node.content != null) {
+    return /*#__PURE__*/_jsx("code", {
+      style: styles.code,
+      children: node.content
+    });
+  }
   return /*#__PURE__*/_jsx("code", {
     style: styles.code,
-    children: node.content ?? renderChildren(node)
+    children: renderChildren(node)
   });
 }
 function LinkRenderer({
diff --git a/node_modules/react-native-enriched-markdown/lib/module/web/renderers/ListRenderers.js b/node_modules/react-native-enriched-markdown/lib/module/web/renderers/ListRenderers.js
index 9433b81..1950089 100644
--- a/node_modules/react-native-enriched-markdown/lib/module/web/renderers/ListRenderers.js
+++ b/node_modules/react-native-enriched-markdown/lib/module/web/renderers/ListRenderers.js
@@ -70,11 +70,17 @@ function ListItemRenderer({
     ...node,
     children: node.children?.filter(isNestedList)
   } : null;
+  if (!isTask) {
+    return /*#__PURE__*/_jsxs("li", {
+      style: listItemStyle(false),
+      children: [renderChildren(inlineNode), nestedNode && renderChildren(nestedNode)]
+    });
+  }
   return /*#__PURE__*/_jsxs("li", {
-    style: listItemStyle(isTask),
+    style: listItemStyle(true),
     children: [/*#__PURE__*/_jsxs("span", {
       style: checkedStyle,
-      children: [isTask && /*#__PURE__*/_jsx("input", {
+      children: [/*#__PURE__*/_jsx("input", {
         type: "checkbox",
         checked: isChecked,
         onChange: handleChange,
diff --git a/node_modules/react-native-enriched-markdown/lib/module/web/renderers/index.js b/node_modules/react-native-enriched-markdown/lib/module/web/renderers/index.js
index 17a8464..3190264 100644
--- a/node_modules/react-native-enriched-markdown/lib/module/web/renderers/index.js
+++ b/node_modules/react-native-enriched-markdown/lib/module/web/renderers/index.js
@@ -24,7 +24,10 @@ export function RenderNode({
   styles,
   callbacks,
   capabilities,
-  parentType
+  parentType,
+  parentAttributes,
+  childIndex,
+  siblingCount
 }) {
   const Renderer = RENDERERS[node.type];
   if (!Renderer) return null;
@@ -34,13 +37,19 @@ export function RenderNode({
     styles: styles,
     callbacks: callbacks,
     capabilities: capabilities,
-    parentType: childNode.type
+    parentType: childNode.type,
+    parentAttributes: childNode.attributes,
+    childIndex: index,
+    siblingCount: childNode.children?.length ?? 0
   }, nodeKey(child, index))) ?? null;
   return /*#__PURE__*/_jsx(Renderer, {
     node: node,
     style: style,
     styles: styles,
     parentType: parentType,
+    parentAttributes: parentAttributes,
+    childIndex: childIndex,
+    siblingCount: siblingCount,
     callbacks: callbacks,
     capabilities: capabilities,
     renderChildren: renderChildren
diff --git a/node_modules/react-native-enriched-markdown/lib/module/web/streamingReveal.d.ts b/node_modules/react-native-enriched-markdown/lib/module/web/streamingReveal.d.ts
new file mode 100644
index 0000000..74504cf
--- /dev/null
+++ b/node_modules/react-native-enriched-markdown/lib/module/web/streamingReveal.d.ts
@@ -0,0 +1 @@
+export * from '../../typescript/src/web/streamingReveal';
diff --git a/node_modules/react-native-enriched-markdown/lib/module/web/streamingReveal.js b/node_modules/react-native-enriched-markdown/lib/module/web/streamingReveal.js
new file mode 100644
index 0000000..bc8dafb
--- /dev/null
+++ b/node_modules/react-native-enriched-markdown/lib/module/web/streamingReveal.js
@@ -0,0 +1,181 @@
+"use strict";
+
+import { createElement } from 'react';
+const COMPARISON_TEXT_NODE_TYPES = new Set(['Text', 'Code', 'LatexMathInline', 'LatexMathDisplay']);
+const NON_REVEALED_PARENT_TYPES = new Set(['Code', 'CodeBlock', 'LatexMathInline', 'LatexMathDisplay']);
+export function readCommonPrefixLength(a, b) {
+  const max = Math.min(a.length, b.length);
+  let index = 0;
+  while (index < max && a[index] === b[index]) {
+    index++;
+  }
+  return index;
+}
+export function readRenderedComparisonText(node) {
+  if (!node) return '';
+  let text = COMPARISON_TEXT_NODE_TYPES.has(node.type) ? node.content ?? '' : '';
+  for (const child of node.children ?? []) {
+    text += readRenderedComparisonText(child);
+  }
+  return text;
+}
+export function markStreamingRevealOffsets(node, cursor = {
+  current: 0
+}) {
+  if (!node) return;
+  if (COMPARISON_TEXT_NODE_TYPES.has(node.type)) {
+    node.streamingRevealStartOffset = cursor.current;
+    cursor.current += (node.content ?? '').length;
+  }
+  for (const child of node.children ?? []) {
+    markStreamingRevealOffsets(child, cursor);
+  }
+}
+export function createStreamingRevealState(params) {
+  if (!params.enabled) return null;
+  return {
+    activeRanges: params.activeRanges
+  };
+}
+function readRevealWordRanges(params) {
+  const ranges = [];
+  const wordPattern = /\S+/g;
+  let match;
+  while ((match = wordPattern.exec(params.text)) != null) {
+    ranges.push({
+      start: params.startOffset + match.index,
+      end: params.startOffset + match.index + match[0].length,
+      expiresAtMs: params.expiresAtMs
+    });
+  }
+  return ranges;
+}
+function mergeRevealRanges(ranges) {
+  const sorted = [...ranges].sort((left, right) => {
+    if (left.start !== right.start) return left.start - right.start;
+    return left.end - right.end;
+  });
+  const merged = [];
+  for (const range of sorted) {
+    const previous = merged[merged.length - 1];
+    if (!previous || range.start > previous.end) {
+      merged.push(range);
+      continue;
+    }
+    merged[merged.length - 1] = {
+      start: previous.start,
+      end: Math.max(previous.end, range.end),
+      expiresAtMs: Math.max(previous.expiresAtMs, range.expiresAtMs)
+    };
+  }
+  return merged;
+}
+const STREAMING_REVEAL_SOURCE_WINDOW_SLACK = 16;
+function resolveStreamingRevealFloor(params) {
+  const hasSourceLengths = typeof params.previousSourceLength === 'number' && Number.isFinite(params.previousSourceLength) && typeof params.currentSourceLength === 'number' && Number.isFinite(params.currentSourceLength);
+  if (!hasSourceLengths) return params.commonPrefixLength;
+
+  // The rendered comparison text is NOT append-only while markdown streams:
+  // completing inline syntax (`**bo` rendered literally, then closing `**` renders
+  // bold `bo`) rewrites the rendered prefix, collapsing the common prefix far
+  // upstream of the tail. Without a bound, every formatting completion re-classified
+  // the whole downstream region as new and replayed the reveal animation each chunk
+  // (constant text flicker on large streaming markdown). The SOURCE markdown IS
+  // append-only during streaming, so genuinely new content is bounded by the appended
+  // source length (with slack for rendered expansion). A pure re-parse with no
+  // appended source reveals nothing new.
+  const appendedSourceLength = Math.max(0, params.currentSourceLength - params.previousSourceLength);
+  if (appendedSourceLength === 0) return params.currentComparisonTextLength;
+  const revealWindow = appendedSourceLength * 2 + STREAMING_REVEAL_SOURCE_WINDOW_SLACK;
+  return Math.max(params.commonPrefixLength, params.currentComparisonTextLength - revealWindow);
+}
+export function updateStreamingRevealRanges(params) {
+  const commonPrefixLength = readCommonPrefixLength(params.previousComparisonText, params.currentComparisonText);
+
+  // A range's start/end are offsets into the rendered comparison text of the
+  // render that CREATED it, and ranges outlive that render (they are carried
+  // forward for ttlMs so the reveal keyframe can finish). The rendered text is
+  // not append-only while markdown streams, so as soon as the common prefix
+  // collapses those offsets denote different characters: a range created over
+  // '**beta' keeps animating as 'beta', and on the raw/repaired markdown flip a
+  // range slides onto words that were painted long ago and replays their
+  // opacity keyframe. Rebase every carried-forward range onto the region that is
+  // byte-identical in both renders — the common prefix — and discard the rest,
+  // which described text that no longer exists. A genuine append leaves the whole
+  // previous text as the common prefix, so ordinary retention is untouched.
+  const retainedRanges = params.activeRanges.filter(range => range.expiresAtMs > params.nowMs && range.start < commonPrefixLength).map(range => ({
+    ...range,
+    end: Math.min(range.end, commonPrefixLength)
+  }));
+
+  // A mount — a remount above all — starts with no baseline: previousComparisonText
+  // is empty while this instance already renders the whole message. Diffing against
+  // '' makes the entire message look appended (the source-append bound cannot help
+  // either: previousSourceLength is 0, so the window spans everything), every word
+  // re-enters the reveal, and the block replays its opacity keyframe. Measured on
+  // web: 1 570 of 2 098 rendered chars below opacity 0.5 for ~155 ms immediately
+  // after the enriched run was replaced mid-stream. Content already rendered when
+  // this instance first compared is not new — seed the baseline and reveal nothing.
+  // Genuine appends after the seed still animate word by word.
+  if (params.previousComparisonText === '' && params.currentComparisonText.length > 0) {
+    return mergeRevealRanges(retainedRanges);
+  }
+  const revealFloor = resolveStreamingRevealFloor({
+    commonPrefixLength,
+    currentComparisonTextLength: params.currentComparisonText.length,
+    previousSourceLength: params.previousSourceLength,
+    currentSourceLength: params.currentSourceLength
+  });
+  if (revealFloor >= params.currentComparisonText.length) {
+    return mergeRevealRanges(retainedRanges);
+  }
+  return mergeRevealRanges([...retainedRanges, ...readRevealWordRanges({
+    text: params.currentComparisonText.slice(revealFloor),
+    startOffset: revealFloor,
+    expiresAtMs: params.nowMs + params.ttlMs
+  })]);
+}
+export function shouldRevealTextForParent(parentType) {
+  return parentType ? !NON_REVEALED_PARENT_TYPES.has(parentType) : true;
+}
+export function splitStreamingRevealTextParts(params) {
+  let cursor = params.startOffset;
+  let rangeIndex = 0;
+  const activeRanges = params.activeRanges;
+  return params.text.split(/(\s+)/).filter(part => part.length > 0).map(part => {
+    const start = cursor;
+    const end = start + part.length;
+    cursor = end;
+    while (rangeIndex < activeRanges.length && activeRanges[rangeIndex].end <= start) {
+      rangeIndex++;
+    }
+    // A suffix can extend a word that is already visible. Keep that word
+    // visible instead of restarting its opacity animation from zero.
+    const isActiveRevealRange = rangeIndex < activeRanges.length && activeRanges[rangeIndex].start <= start;
+    return {
+      text: part,
+      animated: !/^\s+$/.test(part) && isActiveRevealRange,
+      startOffset: start
+    };
+  });
+}
+export function renderStreamingRevealText(params) {
+  const reveal = params.capabilities.streamingReveal;
+  if (!reveal || !params.revealable) {
+    return params.text;
+  }
+  return splitStreamingRevealTextParts({
+    text: params.text,
+    startOffset: params.startOffset,
+    activeRanges: reveal.activeRanges
+  }).map((part, index) => {
+    if (!part.animated) {
+      return part.text;
+    }
+    return createElement('span', {
+      key: `${part.startOffset}:${index}`,
+      'data-happier-enriched-markdown-reveal': 'text'
+    }, part.text);
+  });
+}
+//# sourceMappingURL=streamingReveal.js.map
diff --git a/node_modules/react-native-enriched-markdown/lib/module/web/styles.js b/node_modules/react-native-enriched-markdown/lib/module/web/styles.js
index 1c5a293..875211f 100644
--- a/node_modules/react-native-enriched-markdown/lib/module/web/styles.js
+++ b/node_modules/react-native-enriched-markdown/lib/module/web/styles.js
@@ -101,6 +101,18 @@ function paragraphInBlockquoteStyle(style) {
     marginBottom: 0
   };
 }
+function paragraphInListItemStyle(style) {
+  return {
+    ...baseBlock(style.paragraph),
+    marginTop: 0
+  };
+}
+function paragraphInLastListItemChildStyle(style) {
+  return {
+    ...paragraphInListItemStyle(style),
+    marginBottom: 0
+  };
+}
 function headingStyle(style, level) {
   return baseBlock(style[toHeadingLevel(level)]);
 }
@@ -341,6 +353,8 @@ export function buildStyles(style) {
   const result = {
     paragraph: paragraphStyle(style),
     paragraphInBlockquote: paragraphInBlockquoteStyle(style),
+    paragraphInListItem: paragraphInListItemStyle(style),
+    paragraphInLastListItemChild: paragraphInLastListItemChildStyle(style),
     h1: headingStyle(style, '1'),
     h2: headingStyle(style, '2'),
     h3: headingStyle(style, '3'),
diff --git a/node_modules/react-native-enriched-markdown/lib/typescript/src/EnrichedMarkdownNativeComponent.d.ts b/node_modules/react-native-enriched-markdown/lib/typescript/src/EnrichedMarkdownNativeComponent.d.ts
index 567cc1e..fa903a4 100644
--- a/node_modules/react-native-enriched-markdown/lib/typescript/src/EnrichedMarkdownNativeComponent.d.ts
+++ b/node_modules/react-native-enriched-markdown/lib/typescript/src/EnrichedMarkdownNativeComponent.d.ts
@@ -189,6 +189,7 @@ export interface Md4cFlagsInternal {
      * @default true
      */
     latexMath: boolean;
+    texMathBackslashDelimiters: boolean;
 }
 export interface NativeProps extends ViewProps {
     /**
diff --git a/node_modules/react-native-enriched-markdown/lib/typescript/src/EnrichedMarkdownTextNativeComponent.d.ts b/node_modules/react-native-enriched-markdown/lib/typescript/src/EnrichedMarkdownTextNativeComponent.d.ts
index 4254d5d..8a7ca5c 100644
--- a/node_modules/react-native-enriched-markdown/lib/typescript/src/EnrichedMarkdownTextNativeComponent.d.ts
+++ b/node_modules/react-native-enriched-markdown/lib/typescript/src/EnrichedMarkdownTextNativeComponent.d.ts
@@ -189,6 +189,7 @@ export interface Md4cFlagsInternal {
      * @default true
      */
     latexMath: boolean;
+    texMathBackslashDelimiters: boolean;
 }
 export interface NativeProps extends ViewProps {
     /**
diff --git a/node_modules/react-native-enriched-markdown/lib/typescript/src/index.web.d.ts b/node_modules/react-native-enriched-markdown/lib/typescript/src/index.web.d.ts
index 3513fc6..cc06a72 100644
--- a/node_modules/react-native-enriched-markdown/lib/typescript/src/index.web.d.ts
+++ b/node_modules/react-native-enriched-markdown/lib/typescript/src/index.web.d.ts
@@ -1,4 +1,5 @@
 export { EnrichedMarkdownText, default } from './web/EnrichedMarkdownText';
+export { preloadMarkdownRuntime } from './web/parseMarkdown';
 export type { EnrichedMarkdownTextProps } from './types/MarkdownTextProps.web';
 export type { MarkdownStyle, Md4cFlags } from './types/MarkdownStyle';
 export type { LinkPressEvent, LinkLongPressEvent, TaskListItemPressEvent, } from './types/events';
diff --git a/node_modules/react-native-enriched-markdown/lib/typescript/src/styleUtils.d.ts b/node_modules/react-native-enriched-markdown/lib/typescript/src/styleUtils.d.ts
index 7c5aece..44d8004 100644
--- a/node_modules/react-native-enriched-markdown/lib/typescript/src/styleUtils.d.ts
+++ b/node_modules/react-native-enriched-markdown/lib/typescript/src/styleUtils.d.ts
@@ -1,6 +1,6 @@
-import { type ColorValue } from 'react-native';
+import { type ProcessedColorValue } from 'react-native';
 import type { MarkdownStyle } from './types/MarkdownStyle';
-export declare const normalizeColor: (color: string | undefined) => ColorValue | undefined;
+export declare const normalizeColor: (color: string | undefined) => string | ProcessedColorValue | undefined;
 export declare function mergeSubStyle<T extends Record<string, unknown>>(defaultStyle: T, userStyle?: Partial<T>): T;
 export declare function isStyleEqual(a: MarkdownStyle, b: MarkdownStyle, referenceKeys: readonly string[]): boolean;
 //# sourceMappingURL=styleUtils.d.ts.map
diff --git a/node_modules/react-native-enriched-markdown/lib/typescript/src/types/MarkdownStyle.d.ts b/node_modules/react-native-enriched-markdown/lib/typescript/src/types/MarkdownStyle.d.ts
index b2c9fb9..dff4534 100644
--- a/node_modules/react-native-enriched-markdown/lib/typescript/src/types/MarkdownStyle.d.ts
+++ b/node_modules/react-native-enriched-markdown/lib/typescript/src/types/MarkdownStyle.d.ts
@@ -208,6 +208,11 @@ export interface Md4cFlags {
      * @default true
      */
     latexMath?: boolean;
+    /**
+     * Enable backslash-delimited TeX math spans: \\(..\\) inline and standalone \\[..\\] display math.
+     * @default false
+     */
+    texMathBackslashDelimiters?: boolean;
 }
 export {};
 //# sourceMappingURL=MarkdownStyle.d.ts.map
\ No newline at end of file
diff --git a/node_modules/react-native-enriched-markdown/lib/typescript/src/types/MarkdownTextProps.web.d.ts b/node_modules/react-native-enriched-markdown/lib/typescript/src/types/MarkdownTextProps.web.d.ts
index 609252d..3e6bc94 100644
--- a/node_modules/react-native-enriched-markdown/lib/typescript/src/types/MarkdownTextProps.web.d.ts
+++ b/node_modules/react-native-enriched-markdown/lib/typescript/src/types/MarkdownTextProps.web.d.ts
@@ -59,6 +59,23 @@ export interface EnrichedMarkdownTextProps extends Omit<HTMLAttributes<HTMLDivEl
      * @platform ios, android, web
      */
     selectable?: boolean;
+    /**
+     * Animates newly rendered text while markdown is streaming.
+     * On web this wraps newly rendered prose words in inline spans and leaves
+     * code/math content unwrapped.
+     * @default false
+     * @platform web
+     */
+    streamingAnimation?: boolean;
+    /**
+     * Controls the temporary raw markdown fallback while the async web parser is
+     * still resolving. Use "hidden" for scroll-anchored surfaces that need the
+     * fallback to reserve approximate layout without visibly painting raw
+     * markdown syntax.
+     * @default true
+     * @platform web
+     */
+    renderRawFallback?: boolean | 'hidden';
     /**
      * When false (default), removes trailing margin from the last element to
      * eliminate bottom spacing.
diff --git a/node_modules/react-native-enriched-markdown/lib/typescript/src/web/parseMarkdown.d.ts b/node_modules/react-native-enriched-markdown/lib/typescript/src/web/parseMarkdown.d.ts
index 4643d20..8f43e3b 100644
--- a/node_modules/react-native-enriched-markdown/lib/typescript/src/web/parseMarkdown.d.ts
+++ b/node_modules/react-native-enriched-markdown/lib/typescript/src/web/parseMarkdown.d.ts
@@ -1,4 +1,6 @@
 import type { ASTNode } from './types';
 import type { Md4cFlags } from '../types/MarkdownStyle';
+export declare function preloadMarkdownRuntime(): Promise<void>;
 export declare function parseMarkdown(markdown: string, { underline, latexMath }?: Md4cFlags): Promise<ASTNode>;
+export declare function parseMarkdownSyncIfReady(markdown: string, { underline, latexMath }?: Md4cFlags): ASTNode | null;
 //# sourceMappingURL=parseMarkdown.d.ts.map
\ No newline at end of file
diff --git a/node_modules/react-native-enriched-markdown/lib/typescript/src/web/renderers/index.d.ts b/node_modules/react-native-enriched-markdown/lib/typescript/src/web/renderers/index.d.ts
index 7881f09..42cf7f9 100644
--- a/node_modules/react-native-enriched-markdown/lib/typescript/src/web/renderers/index.d.ts
+++ b/node_modules/react-native-enriched-markdown/lib/typescript/src/web/renderers/index.d.ts
@@ -1,14 +1,5 @@
 import type { ReactNode } from 'react';
-import type { ASTNode, NodeType, RendererCallbacks, RenderCapabilities } from '../types';
-import type { MarkdownStyleInternal } from '../../types/MarkdownStyleInternal';
-import type { Styles } from '../styles';
-export interface RenderNodeProps {
-    node: ASTNode;
-    style: MarkdownStyleInternal;
-    styles: Styles;
-    callbacks: RendererCallbacks;
-    capabilities: RenderCapabilities;
-    parentType?: NodeType;
-}
-export declare function RenderNode({ node, style, styles, callbacks, capabilities, parentType, }: RenderNodeProps): ReactNode;
+import type { RendererProps } from '../types';
+export type RenderNodeProps = Omit<RendererProps, 'renderChildren'>;
+export declare function RenderNode({ node, style, styles, callbacks, capabilities, parentType, parentAttributes, childIndex, siblingCount, }: RenderNodeProps): ReactNode;
 //# sourceMappingURL=index.d.ts.map
diff --git a/node_modules/react-native-enriched-markdown/lib/typescript/src/web/streamingReveal.d.ts b/node_modules/react-native-enriched-markdown/lib/typescript/src/web/streamingReveal.d.ts
new file mode 100644
index 0000000..a2236c2
--- /dev/null
+++ b/node_modules/react-native-enriched-markdown/lib/typescript/src/web/streamingReveal.d.ts
@@ -0,0 +1,45 @@
+import type { ReactNode } from 'react';
+import type { ASTNode, NodeType, RenderCapabilities } from './types';
+export interface StreamingRevealState {
+    activeRanges: readonly StreamingRevealRange[];
+}
+export interface StreamingRevealRange {
+    start: number;
+    end: number;
+    expiresAtMs: number;
+}
+export declare function readCommonPrefixLength(a: string, b: string): number;
+export declare function readRenderedComparisonText(node: ASTNode | null): string;
+export declare function markStreamingRevealOffsets(node: ASTNode | null, cursor?: {
+    current: number;
+}): void;
+export declare function createStreamingRevealState(params: Readonly<{
+    enabled: boolean;
+    activeRanges: readonly StreamingRevealRange[];
+}>): StreamingRevealState | null;
+export declare function updateStreamingRevealRanges(params: Readonly<{
+    activeRanges: readonly StreamingRevealRange[];
+    previousComparisonText: string;
+    currentComparisonText: string;
+    nowMs: number;
+    ttlMs: number;
+    previousSourceLength?: number;
+    currentSourceLength?: number;
+}>): readonly StreamingRevealRange[];
+export declare function shouldRevealTextForParent(parentType?: NodeType): boolean;
+export interface StreamingRevealTextPart {
+    text: string;
+    animated: boolean;
+    startOffset: number;
+}
+export declare function splitStreamingRevealTextParts(params: Readonly<{
+    text: string;
+    startOffset: number;
+    activeRanges: readonly StreamingRevealRange[];
+}>): StreamingRevealTextPart[];
+export declare function renderStreamingRevealText(params: Readonly<{
+    capabilities: RenderCapabilities;
+    text: string;
+    revealable: boolean;
+    startOffset: number;
+}>): ReactNode;
diff --git a/node_modules/react-native-enriched-markdown/lib/typescript/src/web/styles.d.ts b/node_modules/react-native-enriched-markdown/lib/typescript/src/web/styles.d.ts
index d5629f6..2e100d7 100644
--- a/node_modules/react-native-enriched-markdown/lib/typescript/src/web/styles.d.ts
+++ b/node_modules/react-native-enriched-markdown/lib/typescript/src/web/styles.d.ts
@@ -10,6 +10,8 @@ export declare const parseErrorFallbackStyle: CSSProperties;
 export interface Styles {
     paragraph: CSSProperties;
     paragraphInBlockquote: CSSProperties;
+    paragraphInListItem: CSSProperties;
+    paragraphInLastListItemChild: CSSProperties;
     h1: CSSProperties;
     h2: CSSProperties;
     h3: CSSProperties;
diff --git a/node_modules/react-native-enriched-markdown/lib/typescript/src/web/types.d.ts b/node_modules/react-native-enriched-markdown/lib/typescript/src/web/types.d.ts
index 5bf4501..980dfbe 100644
--- a/node_modules/react-native-enriched-markdown/lib/typescript/src/web/types.d.ts
+++ b/node_modules/react-native-enriched-markdown/lib/typescript/src/web/types.d.ts
@@ -3,6 +3,7 @@ import type { MarkdownStyleInternal } from '../types/MarkdownStyleInternal';
 import type { Styles } from './styles';
 import type { LinkPressEvent, LinkLongPressEvent, TaskListItemPressEvent } from '../types/events';
 import type { KaTeXInstance } from './katex';
+import type { StreamingRevealState } from './streamingReveal';
 export type NodeType = 'Document' | 'Paragraph' | 'Text' | 'Link' | 'Heading' | 'LineBreak' | 'Strong' | 'Emphasis' | 'Strikethrough' | 'Underline' | 'Code' | 'Image' | 'Blockquote' | 'UnorderedList' | 'OrderedList' | 'ListItem' | 'CodeBlock' | 'ThematicBreak' | 'Table' | 'TableHead' | 'TableBody' | 'TableRow' | 'TableHeaderCell' | 'TableCell' | 'LatexMathInline' | 'LatexMathDisplay';
 export interface NodeAttributes {
     level?: string;
@@ -29,6 +30,8 @@ export interface ASTNode {
     attributes?: NodeAttributes;
     /** Child nodes; absent on leaf nodes (Text, LineBreak, ThematicBreak). */
     children?: ASTNode[];
+    /** Stamped by markStreamingRevealOffsets() for web streaming reveal. */
+    streamingRevealStartOffset?: number;
 }
 export interface RendererCallbacks {
     onLinkPress?: (event: LinkPressEvent) => void;
@@ -37,12 +40,16 @@ export interface RendererCallbacks {
 }
 export interface RenderCapabilities {
     katex: KaTeXInstance | null;
+    streamingReveal?: StreamingRevealState | null;
 }
 export interface RendererProps {
     node: ASTNode;
     style: MarkdownStyleInternal;
     styles: Styles;
     parentType?: NodeType;
+    parentAttributes?: NodeAttributes;
+    childIndex?: number;
+    siblingCount?: number;
     callbacks: RendererCallbacks;
     capabilities: RenderCapabilities;
     renderChildren: (node: ASTNode) => ReactNode;
diff --git a/node_modules/react-native-enriched-markdown/src/EnrichedMarkdownNativeComponent.ts b/node_modules/react-native-enriched-markdown/src/EnrichedMarkdownNativeComponent.ts
index 06024b4..bf9532a 100644
--- a/node_modules/react-native-enriched-markdown/src/EnrichedMarkdownNativeComponent.ts
+++ b/node_modules/react-native-enriched-markdown/src/EnrichedMarkdownNativeComponent.ts
@@ -224,6 +224,8 @@ export interface Md4cFlagsInternal {
    * @default true
    */
   latexMath: boolean;
+  /** Enable backslash-delimited TeX math delimiters (\\(..\\) and standalone \\[..\\]). */
+  texMathBackslashDelimiters: boolean;
 }
 
 export interface NativeProps extends ViewProps {
diff --git a/node_modules/react-native-enriched-markdown/src/EnrichedMarkdownTextNativeComponent.ts b/node_modules/react-native-enriched-markdown/src/EnrichedMarkdownTextNativeComponent.ts
index e238ecf..e4a6367 100644
--- a/node_modules/react-native-enriched-markdown/src/EnrichedMarkdownTextNativeComponent.ts
+++ b/node_modules/react-native-enriched-markdown/src/EnrichedMarkdownTextNativeComponent.ts
@@ -224,6 +224,8 @@ export interface Md4cFlagsInternal {
    * @default true
    */
   latexMath: boolean;
+  /** Enable backslash-delimited TeX math delimiters (\\(..\\) and standalone \\[..\\]). */
+  texMathBackslashDelimiters: boolean;
 }
 
 export interface NativeProps extends ViewProps {
diff --git a/node_modules/react-native-enriched-markdown/src/index.web.tsx b/node_modules/react-native-enriched-markdown/src/index.web.tsx
index 4b4305c..c6aa6a4 100644
--- a/node_modules/react-native-enriched-markdown/src/index.web.tsx
+++ b/node_modules/react-native-enriched-markdown/src/index.web.tsx
@@ -1,4 +1,5 @@
 export { EnrichedMarkdownText, default } from './web/EnrichedMarkdownText';
+export { preloadMarkdownRuntime } from './web/parseMarkdown';
 export type { EnrichedMarkdownTextProps } from './types/MarkdownTextProps.web';
 export type { MarkdownStyle, Md4cFlags } from './types/MarkdownStyle';
 export type {
diff --git a/node_modules/react-native-enriched-markdown/src/native/EnrichedMarkdownText.tsx b/node_modules/react-native-enriched-markdown/src/native/EnrichedMarkdownText.tsx
index 236c2b7..1e893f5 100644
--- a/node_modules/react-native-enriched-markdown/src/native/EnrichedMarkdownText.tsx
+++ b/node_modules/react-native-enriched-markdown/src/native/EnrichedMarkdownText.tsx
@@ -23,6 +23,7 @@ export type { LinkPressEvent, LinkLongPressEvent, TaskListItemPressEvent };
 const defaultMd4cFlags: Md4cFlags = {
   underline: false,
   latexMath: true,
+  texMathBackslashDelimiters: false,
 };
 
 export const EnrichedMarkdownText = ({
@@ -57,6 +58,7 @@ export const EnrichedMarkdownText = ({
     () => ({
       underline: md4cFlags.underline ?? false,
       latexMath: md4cFlags.latexMath ?? true,
+      texMathBackslashDelimiters: md4cFlags.texMathBackslashDelimiters ?? false,
     }),
     [md4cFlags]
   );
diff --git a/node_modules/react-native-enriched-markdown/src/styleUtils.ts b/node_modules/react-native-enriched-markdown/src/styleUtils.ts
index ad339ba..326e53d 100644
--- a/node_modules/react-native-enriched-markdown/src/styleUtils.ts
+++ b/node_modules/react-native-enriched-markdown/src/styleUtils.ts
@@ -1,9 +1,9 @@
-import { Platform, processColor, type ColorValue } from 'react-native';
+import { Platform, processColor, type ProcessedColorValue } from 'react-native';
 import type { MarkdownStyle } from './types/MarkdownStyle';
 
 export const normalizeColor = (
   color: string | undefined
-): ColorValue | undefined => {
+): string | ProcessedColorValue | undefined => {
   if (!color) return undefined;
   if (Platform.OS === 'web') return color;
   return processColor(color) ?? undefined;
diff --git a/node_modules/react-native-enriched-markdown/src/types/MarkdownStyle.ts b/node_modules/react-native-enriched-markdown/src/types/MarkdownStyle.ts
index c05a97a..f70b8af 100644
--- a/node_modules/react-native-enriched-markdown/src/types/MarkdownStyle.ts
+++ b/node_modules/react-native-enriched-markdown/src/types/MarkdownStyle.ts
@@ -232,4 +232,11 @@ export interface Md4cFlags {
    * @default true
    */
   latexMath?: boolean;
+  /**
+   * Enable backslash-delimited TeX delimiters (\\(..\\) and standalone \\[..\\]).
+   * This is separate from latexMath because CommonMark also uses backslashes
+   * to escape literal parentheses and brackets.
+   * @default false
+   */
+  texMathBackslashDelimiters?: boolean;
 }
diff --git a/node_modules/react-native-enriched-markdown/src/types/MarkdownTextProps.web.ts b/node_modules/react-native-enriched-markdown/src/types/MarkdownTextProps.web.ts
index fa9d401..90d9e21 100644
--- a/node_modules/react-native-enriched-markdown/src/types/MarkdownTextProps.web.ts
+++ b/node_modules/react-native-enriched-markdown/src/types/MarkdownTextProps.web.ts
@@ -65,6 +65,23 @@ export interface EnrichedMarkdownTextProps
    * @platform ios, android, web
    */
   selectable?: boolean;
+  /**
+   * Animates newly rendered text while markdown is streaming.
+   * On web this wraps newly rendered prose words in inline spans and leaves
+   * code/math content unwrapped.
+   * @default false
+   * @platform web
+   */
+  streamingAnimation?: boolean;
+  /**
+   * Controls the temporary raw markdown fallback while the async web parser is
+   * still resolving. Use "hidden" for scroll-anchored surfaces that need the
+   * fallback to reserve approximate layout without visibly painting raw
+   * markdown syntax.
+   * @default true
+   * @platform web
+   */
+  renderRawFallback?: boolean | 'hidden';
   /**
    * When false (default), removes trailing margin from the last element to
    * eliminate bottom spacing.
diff --git a/node_modules/react-native-enriched-markdown/src/web/EnrichedMarkdownText.tsx b/node_modules/react-native-enriched-markdown/src/web/EnrichedMarkdownText.tsx
index 70eb3a4..dec24c2 100644
--- a/node_modules/react-native-enriched-markdown/src/web/EnrichedMarkdownText.tsx
+++ b/node_modules/react-native-enriched-markdown/src/web/EnrichedMarkdownText.tsx
@@ -1,17 +1,40 @@
-import { useState, useEffect, useMemo, type CSSProperties } from 'react';
+import {
+  useState,
+  useEffect,
+  useLayoutEffect,
+  useMemo,
+  useRef,
+  type CSSProperties,
+} from 'react';
 import type { EnrichedMarkdownTextProps } from '../types/MarkdownTextProps.web';
 import { normalizeMarkdownStyle } from '../normalizeMarkdownStyle.web';
 import {
   zeroTrailingMargins,
-  parseErrorFallbackStyle,
   buildStyles,
 } from './styles';
-import { parseMarkdown } from './parseMarkdown';
+import { parseMarkdown, parseMarkdownSyncIfReady } from './parseMarkdown';
 import { RenderNode } from './renderers';
 import type { ASTNode, RendererCallbacks, RenderCapabilities } from './types';
 import { indexTaskItems, markInlineImages } from './utils';
 import { loadKaTeX } from './katex';
 import type { KaTeXInstance } from './katex';
+import {
+  createStreamingRevealState,
+  markStreamingRevealOffsets,
+  readRenderedComparisonText,
+  updateStreamingRevealRanges,
+  type StreamingRevealRange,
+} from './streamingReveal';
+
+const STREAMING_REVEAL_RANGE_TTL_MS = 260;
+const STREAMING_REVEAL_CLEANUP_BUFFER_MS = 16;
+
+function prepareAstForRendering(ast: ASTNode): ASTNode {
+  indexTaskItems(ast);
+  markInlineImages(ast);
+  markStreamingRevealOffsets(ast);
+  return ast;
+}
 
 export const EnrichedMarkdownText = ({
   markdown,
@@ -23,6 +46,8 @@ export const EnrichedMarkdownText = ({
   allowTrailingMargin = false,
   containerStyle,
   selectable = true,
+  streamingAnimation = false,
+  renderRawFallback = true,
   dir,
   ...rest
 }: EnrichedMarkdownTextProps) => {
@@ -30,30 +55,26 @@ export const EnrichedMarkdownText = ({
     () => normalizeMarkdownStyle(markdownStyle),
     [markdownStyle]
   );
+  const { underline = false, latexMath = true, texMathBackslashDelimiters = false } = md4cFlags;
 
+  const syncAst = useMemo(() => {
+    const parsed = parseMarkdownSyncIfReady(markdown, { underline, latexMath, texMathBackslashDelimiters });
+    return parsed ? prepareAstForRendering(parsed) : null;
+  }, [markdown, underline, latexMath, texMathBackslashDelimiters]);
   const [ast, setAst] = useState<ASTNode | null>(null);
   const [katex, setKatex] = useState<KaTeXInstance | null>(null);
   const [parseError, setParseError] = useState<boolean>(false);
 
-  const { underline = false, latexMath = true } = md4cFlags;
-
   useEffect(() => {
+    // A warm synchronous AST is already the rendered authority. Only cold or
+    // failed synchronous parsing needs an asynchronous result publication.
+    if (syncAst) return;
     let cancelled = false;
-
-    const katexPromise = latexMath ? loadKaTeX() : Promise.resolve(null);
-
-    Promise.all([
-      parseMarkdown(markdown, { underline, latexMath }),
-      katexPromise,
-    ])
-      .then(([result, katexInstance]) => {
+    parseMarkdown(markdown, { underline, latexMath, texMathBackslashDelimiters })
+      .then((result) => {
         if (!cancelled) {
-          indexTaskItems(result);
-          markInlineImages(result);
-
           setParseError(false);
-          setKatex(katexInstance);
-          setAst(result);
+          setAst(prepareAstForRendering(result));
         }
       })
       .catch((error) => {
@@ -64,21 +85,112 @@ export const EnrichedMarkdownText = ({
 
           setParseError(true);
           setAst(null);
-          setKatex(null);
         }
       });
 
     return () => {
       cancelled = true;
     };
-  }, [markdown, underline, latexMath]);
+  }, [syncAst, markdown, underline, latexMath, texMathBackslashDelimiters]);
+
+  useEffect(() => {
+    let cancelled = false;
+    if (!latexMath) {
+      setKatex(null);
+      return;
+    }
+    // Optional math readiness is independent of document parsing and streaming.
+    void loadKaTeX().then((instance) => {
+      if (!cancelled) setKatex(instance);
+    });
+    return () => { cancelled = true; };
+  }, [latexMath]);
+
+  const renderedAst = syncAst ?? ast;
 
   const callbacks = useMemo<RendererCallbacks>(
     () => ({ onLinkPress, onLinkLongPress, onTaskListItemPress }),
     [onLinkPress, onLinkLongPress, onTaskListItemPress]
   );
 
-  const capabilities = useMemo<RenderCapabilities>(() => ({ katex }), [katex]);
+  const previousComparisonTextRef = useRef('');
+  const previousSourceLengthRef = useRef(0);
+  const activeRevealRangesRef = useRef<readonly StreamingRevealRange[]>([]);
+  const revealCleanupTimerRef = useRef<ReturnType<typeof setTimeout> | null>(
+    null
+  );
+  const [revealCleanupRevision, setRevealCleanupRevision] = useState(0);
+  const currentComparisonText = useMemo(
+    () => readRenderedComparisonText(renderedAst),
+    [renderedAst]
+  );
+  const activeRevealRanges = useMemo(
+    () =>
+      streamingAnimation
+        ? updateStreamingRevealRanges({
+            activeRanges: activeRevealRangesRef.current,
+            previousComparisonText: previousComparisonTextRef.current,
+            currentComparisonText,
+            nowMs: Date.now(),
+            ttlMs: STREAMING_REVEAL_RANGE_TTL_MS,
+            previousSourceLength: previousSourceLengthRef.current,
+            currentSourceLength: markdown.length,
+          })
+        : [],
+    [currentComparisonText, markdown, revealCleanupRevision, streamingAnimation]
+  );
+  const streamingReveal = createStreamingRevealState({
+    enabled: streamingAnimation,
+    activeRanges: activeRevealRanges,
+  });
+
+  useLayoutEffect(() => {
+    if (!streamingAnimation) {
+      activeRevealRangesRef.current = [];
+      previousComparisonTextRef.current = currentComparisonText;
+      previousSourceLengthRef.current = markdown.length;
+      return;
+    }
+
+    activeRevealRangesRef.current = activeRevealRanges;
+    previousComparisonTextRef.current = currentComparisonText;
+    previousSourceLengthRef.current = markdown.length;
+  }, [activeRevealRanges, currentComparisonText, markdown, streamingAnimation]);
+
+  useEffect(() => {
+    if (revealCleanupTimerRef.current) {
+      clearTimeout(revealCleanupTimerRef.current);
+      revealCleanupTimerRef.current = null;
+    }
+
+    if (!streamingAnimation || activeRevealRanges.length === 0) return;
+
+    const nowMs = Date.now();
+    const nextExpiryMs = Math.min(
+      ...activeRevealRanges.map((range) => range.expiresAtMs)
+    );
+    const delayMs = Math.max(
+      0,
+      nextExpiryMs - nowMs + STREAMING_REVEAL_CLEANUP_BUFFER_MS
+    );
+
+    revealCleanupTimerRef.current = setTimeout(() => {
+      revealCleanupTimerRef.current = null;
+      setRevealCleanupRevision((revision) => revision + 1);
+    }, delayMs);
+
+    return () => {
+      if (revealCleanupTimerRef.current) {
+        clearTimeout(revealCleanupTimerRef.current);
+        revealCleanupTimerRef.current = null;
+      }
+    };
+  }, [activeRevealRanges, streamingAnimation]);
+
+  const capabilities = useMemo<RenderCapabilities>(
+    () => ({ katex, streamingReveal }),
+    [katex, streamingReveal]
+  );
 
   const lastChildStyle = useMemo(
     () =>
@@ -105,17 +217,42 @@ export const EnrichedMarkdownText = ({
     [containerStyle, selectable]
   );
 
-  if (parseError) {
+  if (parseError && !syncAst) {
     return (
       <div style={wrapperStyle} dir={dir} {...rest}>
-        <pre style={parseErrorFallbackStyle}>{markdown}</pre>
+        <p
+          style={{
+            ...lastChildStyles.paragraph,
+            whiteSpace: 'pre-wrap',
+          }}
+        >
+          {markdown}
+        </p>
       </div>
     );
   }
 
-  if (!ast) return null;
+  if (!renderedAst) {
+    const fallbackParagraphStyle = {
+      ...lastChildStyles.paragraph,
+      whiteSpace: 'pre-wrap',
+      ...(renderRawFallback === 'hidden' ? { visibility: 'hidden' as const } : undefined),
+    };
+
+    if (!renderRawFallback) {
+      return <div style={wrapperStyle} dir={dir} {...rest} />;
+    }
+
+    return (
+      <div style={wrapperStyle} dir={dir} {...rest}>
+        <p style={fallbackParagraphStyle}>
+          {markdown}
+        </p>
+      </div>
+    );
+  }
 
-  const children = ast.children ?? [];
+  const children = renderedAst.children ?? [];
   const lastIdx = children.length - 1;
 
   return (
diff --git a/node_modules/react-native-enriched-markdown/src/web/parseMarkdown.ts b/node_modules/react-native-enriched-markdown/src/web/parseMarkdown.ts
index 911e3f1..1dff70c 100644
--- a/node_modules/react-native-enriched-markdown/src/web/parseMarkdown.ts
+++ b/node_modules/react-native-enriched-markdown/src/web/parseMarkdown.ts
@@ -1,37 +1,104 @@
 import type { ASTNode } from './types';
 import type { Md4cFlags } from '../types/MarkdownStyle';
+import createMd4cModule from './wasm/md4c.js';
 
 type ParseFn = (
-  markdown: string,
+  markdownPointer: number,
   underline: number,
-  latexMath: number
+  latexMath: number,
+  texMathBackslashDelimiters: number
 ) => string;
+type Md4cModule = {
+  cwrap(name: string, returnType: string, argTypes: string[]): ParseFn;
+  _malloc(size: number): number;
+  _free(pointer: number): void;
+  stringToUTF8(value: string, outputPointer: number, maxBytesToWrite: number): void;
+  lengthBytesUTF8(value: string): number;
+};
+type Md4cModuleFactory = () => Promise<Md4cModule>;
+type ParserRuntime = Readonly<{
+  parse: ParseFn;
+  wasmModule: Md4cModule;
+}>;
 
 // Caching the Promise (not the resolved value) means concurrent callers share
 // a single WASM initialization — no duplicate loading.
-let parserPromise: Promise<ParseFn> | null = null;
+let parserPromise: Promise<ParserRuntime> | null = null;
+let parserRuntime: ParserRuntime | null = null;
+const PARSE_CACHE_MAX_ENTRIES = 48;
+const PARSE_CACHE_MAX_MARKDOWN_CHARS = 32_000;
+const parseCache = new Map<string, Promise<ASTNode>>();
+const parseResultCache = new Map<string, ASTNode>();
+const WASM_FACTORY_UNAVAILABLE_MESSAGE =
+  'WASM parser module did not export a factory';
+const WASM_INPUT_ALLOCATION_FAILED_MESSAGE =
+  'WASM parser failed to allocate markdown input';
+
+function resolveMd4cModuleFactory(value: unknown): Md4cModuleFactory | null {
+  if (typeof value === 'function') {
+    return value as Md4cModuleFactory;
+  }
+
+  if (typeof value !== 'object' || value === null || !('default' in value)) {
+    return null;
+  }
+
+  return resolveMd4cModuleFactory(
+    (value as { default?: unknown }).default
+  );
+}
 
 // SINGLE_FILE=1 inlines the WASM binary as base64 inside md4c.js, so no
-// network fetch is needed — only a one-time decode + compile on first call.
-function initializeParser(): Promise<ParseFn> {
+// runtime fetch is needed — only a one-time decode + compile on first call.
+function initializeParser(): Promise<ParserRuntime> {
   if (!parserPromise) {
-    parserPromise = import('./wasm/md4c')
-      .then((module) => module.default())
-      .then((wasmModule) =>
-        wasmModule.cwrap('parseMarkdown', 'string', [
-          'string',
+    parserPromise = Promise.resolve()
+      .then(() => {
+        const factory = resolveMd4cModuleFactory(createMd4cModule);
+        if (!factory) {
+          throw new Error(WASM_FACTORY_UNAVAILABLE_MESSAGE);
+        }
+
+        return factory();
+      })
+      .then((wasmModule) => ({
+        parse: wasmModule.cwrap('parseMarkdown', 'string', [
           'number',
           'number',
-        ])
-      )
+          'number',
+          'number',
+        ]),
+        wasmModule,
+      }))
+      .then((runtime) => {
+        parserRuntime = runtime;
+        return runtime;
+      })
       .catch((error) => {
         parserPromise = null;
+        parserRuntime = null;
         throw error;
-      }) as Promise<ParseFn>;
+      }) as Promise<ParserRuntime>;
   }
   return parserPromise;
 }
 
+export async function preloadMarkdownRuntime(): Promise<void> {
+  try {
+    await initializeParser();
+  } catch (error) {
+    if (
+      error instanceof Error &&
+      error.message === WASM_FACTORY_UNAVAILABLE_MESSAGE &&
+      typeof window === 'undefined'
+    ) {
+      return;
+    }
+
+    throw error;
+  }
+}
+
 function isASTNode(value: unknown): value is ASTNode {
   return (
     typeof value === 'object' &&
@@ -41,19 +108,148 @@ function isASTNode(value: unknown): value is ASTNode {
   );
 }
 
+function shouldCacheParseResult(markdown: string): boolean {
+  return markdown.length <= PARSE_CACHE_MAX_MARKDOWN_CHARS;
+}
+
+function buildCacheKey(
+  markdown: string,
+  underline: boolean,
+  latexMath: boolean,
+  texMathBackslashDelimiters: boolean
+): string | null {
+  return shouldCacheParseResult(markdown)
+    ? `${underline ? 1 : 0}\u0000${latexMath ? 1 : 0}\u0000${texMathBackslashDelimiters ? 1 : 0}\u0000${markdown}`
+    : null;
+}
+
+function rememberParsedResult(cacheKey: string | null, ast: ASTNode): void {
+  if (!cacheKey) return;
+
+  parseResultCache.set(cacheKey, cloneAst(ast));
+  parseCache.delete(cacheKey);
+  while (parseResultCache.size > PARSE_CACHE_MAX_ENTRIES) {
+    const oldestKey = parseResultCache.keys().next().value;
+    if (typeof oldestKey !== 'string') break;
+    parseResultCache.delete(oldestKey);
+  }
+}
+
+function readCachedParsedResult(cacheKey: string | null): ASTNode | null {
+  if (!cacheKey) return null;
+
+  const cached = parseResultCache.get(cacheKey);
+  if (!cached) return null;
+  parseResultCache.delete(cacheKey);
+  parseResultCache.set(cacheKey, cached);
+  return cloneAst(cached);
+}
+
+function parseMarkdownWithHeapInput(
+  runtime: ParserRuntime,
+  markdown: string,
+  underline: boolean,
+  latexMath: boolean,
+  texMathBackslashDelimiters: boolean
+): string {
+  const { wasmModule } = runtime;
+  const inputBytes = wasmModule.lengthBytesUTF8(markdown) + 1;
+  const inputPointer = wasmModule._malloc(inputBytes);
+  if (!inputPointer) {
+    throw new Error(WASM_INPUT_ALLOCATION_FAILED_MESSAGE);
+  }
+
+  try {
+    wasmModule.stringToUTF8(markdown, inputPointer, inputBytes);
+    return runtime.parse(
+      inputPointer,
+      underline ? 1 : 0,
+      latexMath ? 1 : 0,
+      texMathBackslashDelimiters ? 1 : 0
+    );
+  } finally {
+    wasmModule._free(inputPointer);
+  }
+}
+
 export async function parseMarkdown(
   markdown: string,
-  { underline = false, latexMath = true }: Md4cFlags = {}
+  { underline = false, latexMath = true, texMathBackslashDelimiters = false }: Md4cFlags = {}
 ): Promise<ASTNode> {
-  const parse = await initializeParser();
+  const cacheKey = buildCacheKey(markdown, underline, latexMath, texMathBackslashDelimiters);
+  const cachedResult = readCachedParsedResult(cacheKey);
+  if (cachedResult) return cachedResult;
 
-  const result: unknown = JSON.parse(
-    parse(markdown, underline ? 1 : 0, latexMath ? 1 : 0)
-  );
+  if (cacheKey) {
+    const cached = parseCache.get(cacheKey);
+    if (cached) {
+      parseCache.delete(cacheKey);
+      parseCache.set(cacheKey, cached);
+      return cloneAst(await cached);
+    }
+  }
+
+  const parsed = initializeParser().then((runtime) => {
+    const result: unknown = JSON.parse(
+      parseMarkdownWithHeapInput(runtime, markdown, underline, latexMath, texMathBackslashDelimiters)
+    );
 
-  if (!isASTNode(result)) {
-    throw new Error('WASM parser returned invalid AST');
+    if (!isASTNode(result)) {
+      throw new Error('WASM parser returned invalid AST');
+    }
+
+    rememberParsedResult(cacheKey, result);
+    return result;
+  });
+
+  if (cacheKey) {
+    parseCache.set(cacheKey, parsed);
+    while (parseCache.size > PARSE_CACHE_MAX_ENTRIES) {
+      const oldestKey = parseCache.keys().next().value;
+      if (typeof oldestKey !== 'string') break;
+      parseCache.delete(oldestKey);
+    }
+  }
+
+  try {
+    return cloneAst(await parsed);
+  } catch (error) {
+    if (cacheKey) {
+      parseCache.delete(cacheKey);
+    }
+    throw error;
+  }
+}
+
+export function parseMarkdownSyncIfReady(
+  markdown: string,
+  { underline = false, latexMath = true, texMathBackslashDelimiters = false }: Md4cFlags = {}
+): ASTNode | null {
+  const cacheKey = buildCacheKey(markdown, underline, latexMath, texMathBackslashDelimiters);
+  const cachedResult = readCachedParsedResult(cacheKey);
+  if (cachedResult) return cachedResult;
+  if (!parserRuntime) return null;
+
+  try {
+    const result: unknown = JSON.parse(
+      parseMarkdownWithHeapInput(parserRuntime, markdown, underline, latexMath, texMathBackslashDelimiters)
+    );
+
+    if (!isASTNode(result)) {
+      throw new Error('WASM parser returned invalid AST');
+    }
+
+    rememberParsedResult(cacheKey, result);
+    return cloneAst(result);
+  } catch (error) {
+    if (cacheKey) {
+      parseCache.delete(cacheKey);
+      parseResultCache.delete(cacheKey);
+    }
+    return null;
   }
+}
 
-  return result;
+function cloneAst(ast: ASTNode): ASTNode {
+  return JSON.parse(JSON.stringify(ast)) as ASTNode;
 }
diff --git a/node_modules/react-native-enriched-markdown/src/web/renderers/BlockRenderers.tsx b/node_modules/react-native-enriched-markdown/src/web/renderers/BlockRenderers.tsx
index 28c5386..f620023 100644
--- a/node_modules/react-native-enriched-markdown/src/web/renderers/BlockRenderers.tsx
+++ b/node_modules/react-native-enriched-markdown/src/web/renderers/BlockRenderers.tsx
@@ -7,6 +7,9 @@ function ParagraphRenderer({
   node,
   styles,
   parentType,
+  parentAttributes,
+  childIndex,
+  siblingCount,
   renderChildren,
 }: RendererProps) {
   const isImageOnly =
@@ -18,7 +21,19 @@ function ParagraphRenderer({
   }
 
   if (parentType === 'ListItem') {
-    return <span>{renderChildren(node)}</span>;
+    if (parentAttributes?.isTask === 'true') {
+      return <span>{renderChildren(node)}</span>;
+    }
+
+    const isLastListItemChild =
+      childIndex !== undefined && siblingCount !== undefined
+        ? childIndex >= siblingCount - 1
+        : false;
+    const paragraphStyle = isLastListItemChild
+      ? styles.paragraphInLastListItemChild
+      : styles.paragraphInListItem;
+
+    return <p style={paragraphStyle}>{renderChildren(node)}</p>;
   }
 
   return <p style={styles.paragraph}>{renderChildren(node)}</p>;
diff --git a/node_modules/react-native-enriched-markdown/src/web/renderers/InlineRenderers.tsx b/node_modules/react-native-enriched-markdown/src/web/renderers/InlineRenderers.tsx
index f25cb2a..b6c021e 100644
--- a/node_modules/react-native-enriched-markdown/src/web/renderers/InlineRenderers.tsx
+++ b/node_modules/react-native-enriched-markdown/src/web/renderers/InlineRenderers.tsx
@@ -2,9 +2,22 @@ import type { MouseEvent } from 'react';
 import type { RendererProps, RendererMap } from '../types';
 import { extractNodeText } from '../utils';
 import { KaTeXRenderer } from './KaTeXRenderer';
+import {
+  renderStreamingRevealText,
+  shouldRevealTextForParent,
+} from '../streamingReveal';
 
-function TextRenderer({ node }: RendererProps) {
-  return <>{node.content ?? ''}</>;
+function TextRenderer({ node, parentType, capabilities }: RendererProps) {
+  return (
+    <>
+      {renderStreamingRevealText({
+        capabilities,
+        text: node.content ?? '',
+        revealable: shouldRevealTextForParent(parentType),
+        startOffset: node.streamingRevealStartOffset ?? 0,
+      })}
+    </>
+  );
 }
 
 function LineBreakRenderer(_props: RendererProps) {
@@ -32,8 +45,12 @@ function UnderlineRenderer({ node, styles, renderChildren }: RendererProps) {
 }
 
 function CodeRenderer({ node, styles, renderChildren }: RendererProps) {
+  if (node.content != null) {
+    return <code style={styles.code}>{node.content}</code>;
+  }
+
   return (
-    <code style={styles.code}>{node.content ?? renderChildren(node)}</code>
+    <code style={styles.code}>{renderChildren(node)}</code>
   );
 }
 
diff --git a/node_modules/react-native-enriched-markdown/src/web/renderers/ListRenderers.tsx b/node_modules/react-native-enriched-markdown/src/web/renderers/ListRenderers.tsx
index dfe69cb..4d4e7c2 100644
--- a/node_modules/react-native-enriched-markdown/src/web/renderers/ListRenderers.tsx
+++ b/node_modules/react-native-enriched-markdown/src/web/renderers/ListRenderers.tsx
@@ -84,18 +84,25 @@ function ListItemRenderer({
     ? { ...node, children: node.children?.filter(isNestedList) }
     : null;
 
+  if (!isTask) {
+    return (
+      <li style={listItemStyle(false)}>
+        {renderChildren(inlineNode)}
+        {nestedNode && renderChildren(nestedNode)}
+      </li>
+    );
+  }
+
   return (
-    <li style={listItemStyle(isTask)}>
+    <li style={listItemStyle(true)}>
       <span style={checkedStyle}>
-        {isTask && (
-          <input
-            type="checkbox"
-            checked={isChecked}
-            onChange={handleChange}
-            style={styles.taskCheckbox}
-            aria-label={`Task: ${taskText}`}
-          />
-        )}
+        <input
+          type="checkbox"
+          checked={isChecked}
+          onChange={handleChange}
+          style={styles.taskCheckbox}
+          aria-label={`Task: ${taskText}`}
+        />
         {renderChildren(inlineNode)}
       </span>
       {nestedNode && renderChildren(nestedNode)}
diff --git a/node_modules/react-native-enriched-markdown/src/web/renderers/index.tsx b/node_modules/react-native-enriched-markdown/src/web/renderers/index.tsx
index dca24cf..a59dc94 100644
--- a/node_modules/react-native-enriched-markdown/src/web/renderers/index.tsx
+++ b/node_modules/react-native-enriched-markdown/src/web/renderers/index.tsx
@@ -1,13 +1,9 @@
 import type { ReactNode } from 'react';
 import type {
   ASTNode,
-  NodeType,
-  RendererCallbacks,
-  RenderCapabilities,
+  RendererProps,
   RendererMap,
 } from '../types';
-import type { MarkdownStyleInternal } from '../../types/MarkdownStyleInternal';
-import type { Styles } from '../styles';
 import { blockRenderers } from './BlockRenderers';
 import { inlineRenderers } from './InlineRenderers';
 import { listRenderers } from './ListRenderers';
@@ -28,14 +24,7 @@ const RENDERERS: RendererMap = {
   ...tableRenderers,
 };
 
-export interface RenderNodeProps {
-  node: ASTNode;
-  style: MarkdownStyleInternal;
-  styles: Styles;
-  callbacks: RendererCallbacks;
-  capabilities: RenderCapabilities;
-  parentType?: NodeType;
-}
+export type RenderNodeProps = Omit<RendererProps, 'renderChildren'>;
 
 export function RenderNode({
   node,
@@ -44,6 +33,9 @@ export function RenderNode({
   callbacks,
   capabilities,
   parentType,
+  parentAttributes,
+  childIndex,
+  siblingCount,
 }: RenderNodeProps): ReactNode {
   const Renderer = RENDERERS[node.type];
   if (!Renderer) return null;
@@ -58,6 +50,9 @@ export function RenderNode({
         callbacks={callbacks}
         capabilities={capabilities}
         parentType={childNode.type}
+        parentAttributes={childNode.attributes}
+        childIndex={index}
+        siblingCount={childNode.children?.length ?? 0}
       />
     )) ?? null;
 
@@ -67,6 +62,9 @@ export function RenderNode({
       style={style}
       styles={styles}
       parentType={parentType}
+      parentAttributes={parentAttributes}
+      childIndex={childIndex}
+      siblingCount={siblingCount}
       callbacks={callbacks}
       capabilities={capabilities}
       renderChildren={renderChildren}
diff --git a/node_modules/react-native-enriched-markdown/src/web/streamingReveal.ts b/node_modules/react-native-enriched-markdown/src/web/streamingReveal.ts
new file mode 100644
index 0000000..f31bffe
--- /dev/null
+++ b/node_modules/react-native-enriched-markdown/src/web/streamingReveal.ts
@@ -0,0 +1,307 @@
+import { createElement, type ReactNode } from 'react';
+import type { ASTNode, NodeType, RenderCapabilities } from './types';
+
+const COMPARISON_TEXT_NODE_TYPES: ReadonlySet<NodeType> = new Set([
+  'Text',
+  'Code',
+  'LatexMathInline',
+  'LatexMathDisplay',
+]);
+
+const NON_REVEALED_PARENT_TYPES: ReadonlySet<NodeType> = new Set([
+  'Code',
+  'CodeBlock',
+  'LatexMathInline',
+  'LatexMathDisplay',
+]);
+
+export interface StreamingRevealState {
+  activeRanges: readonly StreamingRevealRange[];
+}
+
+export interface StreamingRevealRange {
+  start: number;
+  end: number;
+  expiresAtMs: number;
+}
+
+export function readCommonPrefixLength(a: string, b: string): number {
+  const max = Math.min(a.length, b.length);
+  let index = 0;
+  while (index < max && a[index] === b[index]) {
+    index++;
+  }
+  return index;
+}
+
+export function readRenderedComparisonText(node: ASTNode | null): string {
+  if (!node) return '';
+
+  let text = COMPARISON_TEXT_NODE_TYPES.has(node.type) ? node.content ?? '' : '';
+  for (const child of node.children ?? []) {
+    text += readRenderedComparisonText(child);
+  }
+  return text;
+}
+
+export function markStreamingRevealOffsets(
+  node: ASTNode | null,
+  cursor: { current: number } = { current: 0 }
+): void {
+  if (!node) return;
+
+  if (COMPARISON_TEXT_NODE_TYPES.has(node.type)) {
+    node.streamingRevealStartOffset = cursor.current;
+    cursor.current += (node.content ?? '').length;
+  }
+
+  for (const child of node.children ?? []) {
+    markStreamingRevealOffsets(child, cursor);
+  }
+}
+
+export function createStreamingRevealState(params: Readonly<{
+  enabled: boolean;
+  activeRanges: readonly StreamingRevealRange[];
+}>): StreamingRevealState | null {
+  if (!params.enabled) return null;
+
+  return {
+    activeRanges: params.activeRanges,
+  };
+}
+
+function readRevealWordRanges(params: Readonly<{
+  text: string;
+  startOffset: number;
+  expiresAtMs: number;
+}>): StreamingRevealRange[] {
+  const ranges: StreamingRevealRange[] = [];
+  const wordPattern = /\S+/g;
+  let match: RegExpExecArray | null;
+
+  while ((match = wordPattern.exec(params.text)) != null) {
+    ranges.push({
+      start: params.startOffset + match.index,
+      end: params.startOffset + match.index + match[0].length,
+      expiresAtMs: params.expiresAtMs,
+    });
+  }
+
+  return ranges;
+}
+
+function mergeRevealRanges(
+  ranges: readonly StreamingRevealRange[]
+): StreamingRevealRange[] {
+  const sorted = [...ranges].sort((left, right) => {
+    if (left.start !== right.start) return left.start - right.start;
+    return left.end - right.end;
+  });
+  const merged: StreamingRevealRange[] = [];
+
+  for (const range of sorted) {
+    const previous = merged[merged.length - 1];
+    if (!previous || range.start > previous.end) {
+      merged.push(range);
+      continue;
+    }
+
+    merged[merged.length - 1] = {
+      start: previous.start,
+      end: Math.max(previous.end, range.end),
+      expiresAtMs: Math.max(previous.expiresAtMs, range.expiresAtMs),
+    };
+  }
+
+  return merged;
+}
+
+const STREAMING_REVEAL_SOURCE_WINDOW_SLACK = 16;
+
+function resolveStreamingRevealFloor(params: Readonly<{
+  commonPrefixLength: number;
+  currentComparisonTextLength: number;
+  previousSourceLength?: number;
+  currentSourceLength?: number;
+}>): number {
+  const hasSourceLengths =
+    typeof params.previousSourceLength === 'number' &&
+    Number.isFinite(params.previousSourceLength) &&
+    typeof params.currentSourceLength === 'number' &&
+    Number.isFinite(params.currentSourceLength);
+  if (!hasSourceLengths) return params.commonPrefixLength;
+
+  // The rendered comparison text is NOT append-only while markdown streams:
+  // completing inline syntax (`**bo` rendered literally, then closing `**` renders
+  // bold `bo`) rewrites the rendered prefix, collapsing the common prefix far
+  // upstream of the tail. Without a bound, every formatting completion re-classified
+  // the whole downstream region as new and replayed the reveal animation each chunk
+  // (constant text flicker on large streaming markdown). The SOURCE markdown IS
+  // append-only during streaming, so genuinely new content is bounded by the appended
+  // source length (with slack for rendered expansion). A pure re-parse with no
+  // appended source reveals nothing new.
+  const appendedSourceLength = Math.max(
+    0,
+    (params.currentSourceLength as number) - (params.previousSourceLength as number)
+  );
+  if (appendedSourceLength === 0) return params.currentComparisonTextLength;
+  const revealWindow = appendedSourceLength * 2 + STREAMING_REVEAL_SOURCE_WINDOW_SLACK;
+  return Math.max(
+    params.commonPrefixLength,
+    params.currentComparisonTextLength - revealWindow
+  );
+}
+
+export function updateStreamingRevealRanges(params: Readonly<{
+  activeRanges: readonly StreamingRevealRange[];
+  previousComparisonText: string;
+  currentComparisonText: string;
+  nowMs: number;
+  ttlMs: number;
+  previousSourceLength?: number;
+  currentSourceLength?: number;
+}>): readonly StreamingRevealRange[] {
+  const commonPrefixLength = readCommonPrefixLength(
+    params.previousComparisonText,
+    params.currentComparisonText
+  );
+
+  // A range's start/end are offsets into the rendered comparison text of the
+  // render that CREATED it, and ranges outlive that render (they are carried
+  // forward for ttlMs so the reveal keyframe can finish). The rendered text is
+  // not append-only while markdown streams, so as soon as the common prefix
+  // collapses those offsets denote different characters: a range created over
+  // '**beta' keeps animating as 'beta', and on the raw/repaired markdown flip a
+  // range slides onto words that were painted long ago and replays their
+  // opacity keyframe. Rebase every carried-forward range onto the region that is
+  // byte-identical in both renders — the common prefix — and discard the rest,
+  // which described text that no longer exists. A genuine append leaves the whole
+  // previous text as the common prefix, so ordinary retention is untouched.
+  const retainedRanges = params.activeRanges.filter((range) =>
+    range.expiresAtMs > params.nowMs &&
+    range.start < commonPrefixLength
+  ).map((range) => ({
+    ...range,
+    end: Math.min(range.end, commonPrefixLength),
+  }));
+
+  // A mount — a remount above all — starts with no baseline: previousComparisonText
+  // is empty while this instance already renders the whole message. Diffing against
+  // '' makes the entire message look appended (the source-append bound cannot help
+  // either: previousSourceLength is 0, so the window spans everything), every word
+  // re-enters the reveal, and the block replays its opacity keyframe. Measured on
+  // web: 1 570 of 2 098 rendered chars below opacity 0.5 for ~155 ms immediately
+  // after the enriched run was replaced mid-stream. Content already rendered when
+  // this instance first compared is not new — seed the baseline and reveal nothing.
+  // Genuine appends after the seed still animate word by word.
+  if (
+    params.previousComparisonText === '' &&
+    params.currentComparisonText.length > 0
+  ) {
+    return mergeRevealRanges(retainedRanges);
+  }
+
+  const revealFloor = resolveStreamingRevealFloor({
+    commonPrefixLength,
+    currentComparisonTextLength: params.currentComparisonText.length,
+    previousSourceLength: params.previousSourceLength,
+    currentSourceLength: params.currentSourceLength,
+  });
+  if (revealFloor >= params.currentComparisonText.length) {
+    return mergeRevealRanges(retainedRanges);
+  }
+
+  return mergeRevealRanges([
+    ...retainedRanges,
+    ...readRevealWordRanges({
+      text: params.currentComparisonText.slice(revealFloor),
+      startOffset: revealFloor,
+      expiresAtMs: params.nowMs + params.ttlMs,
+    }),
+  ]);
+}
+
+export function shouldRevealTextForParent(parentType?: NodeType): boolean {
+  return parentType ? !NON_REVEALED_PARENT_TYPES.has(parentType) : true;
+}
+
+export interface StreamingRevealTextPart {
+  text: string;
+  animated: boolean;
+  startOffset: number;
+}
+
+/**
+ * SHARED word classifier for streaming text reveal — the single owner of the
+ * "which words of this text animate in" decision. Consumed by the enriched AST
+ * renderer below (range-driven) and by the host app's plain-text
+ * `StreamingTextReveal` (single appended-suffix range), so the two streaming
+ * surfaces cannot drift apart in word semantics.
+ */
+export function splitStreamingRevealTextParts(params: Readonly<{
+  text: string;
+  startOffset: number;
+  activeRanges: readonly StreamingRevealRange[];
+}>): StreamingRevealTextPart[] {
+  let cursor = params.startOffset;
+  let rangeIndex = 0;
+  const activeRanges = params.activeRanges;
+  return params.text
+    .split(/(\s+)/)
+    .filter((part) => part.length > 0)
+    .map((part) => {
+      const start = cursor;
+      const end = start + part.length;
+      cursor = end;
+
+      while (
+        rangeIndex < activeRanges.length &&
+        activeRanges[rangeIndex]!.end <= start
+      ) {
+        rangeIndex++;
+      }
+
+      const isActiveRevealRange =
+        rangeIndex < activeRanges.length &&
+        // A suffix can extend a word that is already visible. Keep that word
+        // visible instead of restarting its opacity animation from zero.
+        activeRanges[rangeIndex]!.start <= start;
+
+      return {
+        text: part,
+        animated: !/^\s+$/.test(part) && isActiveRevealRange,
+        startOffset: start,
+      };
+    });
+}
+
+export function renderStreamingRevealText(params: Readonly<{
+  capabilities: RenderCapabilities;
+  text: string;
+  revealable: boolean;
+  startOffset: number;
+}>): ReactNode {
+  const reveal = params.capabilities.streamingReveal;
+  if (!reveal || !params.revealable) {
+    return params.text;
+  }
+
+  return splitStreamingRevealTextParts({
+    text: params.text,
+    startOffset: params.startOffset,
+    activeRanges: reveal.activeRanges,
+  }).map((part, index) => {
+    if (!part.animated) {
+      return part.text;
+    }
+    return createElement(
+      'span',
+      {
+        key: `${part.startOffset}:${index}`,
+        'data-happier-enriched-markdown-reveal': 'text',
+      },
+      part.text
+    );
+  });
+}
diff --git a/node_modules/react-native-enriched-markdown/src/web/styles.ts b/node_modules/react-native-enriched-markdown/src/web/styles.ts
index 4d0ff8e..10b7315 100644
--- a/node_modules/react-native-enriched-markdown/src/web/styles.ts
+++ b/node_modules/react-native-enriched-markdown/src/web/styles.ts
@@ -106,6 +106,16 @@ function paragraphInBlockquoteStyle(
   return { ...baseBlock(style.paragraph), marginTop: 0, marginBottom: 0 };
 }
 
+function paragraphInListItemStyle(style: MarkdownStyleInternal): CSSProperties {
+  return { ...baseBlock(style.paragraph), marginTop: 0 };
+}
+
+function paragraphInLastListItemChildStyle(
+  style: MarkdownStyleInternal
+): CSSProperties {
+  return { ...paragraphInListItemStyle(style), marginBottom: 0 };
+}
+
 function headingStyle(
   style: MarkdownStyleInternal,
   level: string
@@ -382,6 +392,8 @@ export const parseErrorFallbackStyle: CSSProperties = {
 export interface Styles {
   paragraph: CSSProperties;
   paragraphInBlockquote: CSSProperties;
+  paragraphInListItem: CSSProperties;
+  paragraphInLastListItemChild: CSSProperties;
   h1: CSSProperties;
   h2: CSSProperties;
   h3: CSSProperties;
@@ -424,6 +436,8 @@ export function buildStyles(style: MarkdownStyleInternal): Styles {
   const result: Styles = {
     paragraph: paragraphStyle(style),
     paragraphInBlockquote: paragraphInBlockquoteStyle(style),
+    paragraphInListItem: paragraphInListItemStyle(style),
+    paragraphInLastListItemChild: paragraphInLastListItemChildStyle(style),
     h1: headingStyle(style, '1'),
     h2: headingStyle(style, '2'),
     h3: headingStyle(style, '3'),
diff --git a/node_modules/react-native-enriched-markdown/src/web/types.ts b/node_modules/react-native-enriched-markdown/src/web/types.ts
index 3a297c5..95da4b2 100644
--- a/node_modules/react-native-enriched-markdown/src/web/types.ts
+++ b/node_modules/react-native-enriched-markdown/src/web/types.ts
@@ -7,6 +7,7 @@ import type {
   TaskListItemPressEvent,
 } from '../types/events';
 import type { KaTeXInstance } from './katex';
+import type { StreamingRevealState } from './streamingReveal';
 
 export type NodeType =
   | 'Document'
@@ -62,6 +63,8 @@ export interface ASTNode {
   attributes?: NodeAttributes;
   /** Child nodes; absent on leaf nodes (Text, LineBreak, ThematicBreak). */
   children?: ASTNode[];
+  /** Stamped by markStreamingRevealOffsets() for web streaming reveal. */
+  streamingRevealStartOffset?: number;
 }
 
 export interface RendererCallbacks {
@@ -72,6 +75,7 @@ export interface RendererCallbacks {
 
 export interface RenderCapabilities {
   katex: KaTeXInstance | null;
+  streamingReveal?: StreamingRevealState | null;
 }
 
 export interface RendererProps {
@@ -79,6 +83,9 @@ export interface RendererProps {
   style: MarkdownStyleInternal;
   styles: Styles;
   parentType?: NodeType;
+  parentAttributes?: NodeAttributes;
+  childIndex?: number;
+  siblingCount?: number;
   callbacks: RendererCallbacks;
   capabilities: RenderCapabilities;
   renderChildren: (node: ASTNode) => ReactNode;
